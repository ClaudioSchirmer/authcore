// Code generated by omnicore-gen. DO NOT EDIT.
//
// The TenantStatus enumeration (3 members).
//
// entity:     Tenant
// spec:       specs/omnicore-gen/tenant.omnicore.yaml
// generator:  omnicore-gen
// generated:  2026-08-29
// checksum:   sha256:38e53d115a36dc35b57b2565f518375d450bc2132afe59f71311b60e62eb9846
//
// The line above is the Go convention that tells linters to skip this file.
// It is NOT a rule that the code may not change: this file is yours, in your
// repository, and improving it is ordinary work. What the generator asks is
// only that it be told.
//
// Changing the SPEC and regenerating is the cheaper of the two edits — it
// survives every later run. When the spec cannot say what you need and the
// framework can, edit this file: ask whoever owns the service first, and
// then run `omnicore-gen adopt <this path> -why '<what the spec could not
// express>'`. Adopting re-hashes the file as it now stands, so regeneration
// KEEPS your edit — and every later run prints the file as adopted, so it
// stays visible instead of becoming a surprise.
//
// Without that adopt, the next run notices the checksum no longer matches
// and REFUSES rather than overwriting — your work is never lost, but the
// run stops for whoever meets it.

package vos

import "github.com/ClaudioSchirmer/omnicore/domain"

// TenantStatus is a closed set of values.
//
// The commercial lifecycle state of a tenant.
//
// Go has no enum keyword, so this is the framework's stand-in: a named type
// plus a declared member list, which the framework checks on every write.
// There is no IsValid here — membership is not a rule this type writes.
type TenantStatus string

// The values are EXPLICIT, never an implicit sequence: inserting a member into
// a generated sequence would silently renumber the ones after it, and the
// stored data would no longer mean what it used to.
const (
	// TenantStatusUnknown is the zero value: an out-of-set input lands here rather than
	// passing as a member.
	TenantStatusUnknown   TenantStatus = ""
	TenantStatusTrial     TenantStatus = "trial"
	TenantStatusActive    TenantStatus = "active"
	TenantStatusSuspended TenantStatus = "suspended"
)

var tenantStatusMembers = []TenantStatus{
	TenantStatusTrial,
	TenantStatusActive,
	TenantStatusSuspended,
}

func (v TenantStatus) Value() string { return string(v) }

// Values is what the framework checks membership against. The unknown sentinel
// is deliberately absent from it.
func (v TenantStatus) Values() []TenantStatus { return tenantStatusMembers }

func (v TenantStatus) UnknownNotification() domain.Notification {
	return UnknownTenantStatusNotification{}
}
