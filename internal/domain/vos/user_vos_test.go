// Code generated by omnicore-gen. DO NOT EDIT.
//
// Tests for 2 value object(s).
//
// entity:     User
// spec:       specs/omnicore-gen/user.omnicore.yaml
// generator:  omnicore-gen
// generated:  2026-08-26
// checksum:   sha256:590d27f9a89b2a8b1056fa7c60534736261fa946bab2788831c4e6f864ec2e1e
//
// The checksum covers this file with the checksum line itself blanked. The
// generator recomputes it before every write: if it does not match, the file
// was changed by hand and the run REFUSES it — your change is neither
// overwritten nor updated. Change the spec and regenerate instead; to keep a
// deliberate edit, run omnicore-gen adopt on this path.

package vos

import (
	"strings"

	"testing"

	"github.com/ClaudioSchirmer/omnicore/domain"
)

// Email validates its own rule, so the aggregate never has to.
func TestEmailRejectsInvalid(t *testing.T) {
	ctx := domain.NewNotificationContext("Email")
	if Email("!!not-valid!!").IsValid("Email", ctx) {
		t.Error("an invalid value was accepted")
	}
}

// Email refuses every shape its rule names.
//
// One case per bound, because a rule that checks only the first one still
// passes a single negative test.
func TestEmailRefusesEachWay(t *testing.T) {
	ctx := domain.NewNotificationContext("Email")
	for what, v := range map[string]Email{
		"empty":    Email(""),
		"too long": Email(strings.Repeat("a", 255)),
	} {
		if v.IsValid("Email", ctx) {
			t.Errorf("a value that is %s was accepted", what)
		}
	}
}

// Email accepts a well-formed value and reads it back.
func TestEmailAcceptsValid(t *testing.T) {
	ctx := domain.NewNotificationContext("Email")
	v := Email("maria@acme.com")
	if !v.IsValid("Email", ctx) {
		t.Error("a well-formed value was refused")
	}
	if v.Value() != "maria@acme.com" {
		t.Error("the value did not read back unchanged")
	}
}

// UserStatus declares its members; the framework validates membership against
// exactly this set.
func TestUserStatusMembers(t *testing.T) {
	members := UserStatus("").Values()
	if len(members) != 2 {
		t.Fatalf("expected 2 members, got %d", len(members))
	}
	// The unknown sentinel must NOT be a member: it is where an out-of-set
	// value lands, so accepting it would defeat the whole check.
	for _, m := range members {
		if m == UserStatusUnknown {
			t.Error("the unknown sentinel must not be a declared member")
		}
	}
}

// A declared member of UserStatus is accepted and reads back unchanged.
//
// The pair matters: a value object that rejects everything passes the negative
// test below and is still broken.
// A value outside UserStatus's set is not one of its members.
//
// An enum declares no rule of its own: the framework decides membership from
// Values(), and answers with the unknown notification. So what is under test
// is the SET — that a member is in it and a stranger is not.
func TestUserStatusMembership(t *testing.T) {
	in := func(v UserStatus) bool {
		for _, m := range v.Values() {
			if m == v {
				return true
			}
		}
		return false
	}
	if !in(UserStatusActive) {
		t.Error("a declared member is not in its own set")
	}
	if in(UserStatus("!!not-valid!!")) {
		t.Error("a value outside the set was found in it")
	}
	if UserStatusActive.Value() == "" {
		t.Error("a member reads back empty")
	}
	if UserStatusActive.UnknownNotification() == nil {
		t.Error("the enum has no notification for an unknown value")
	}
}
