// Code generated by omnicore-gen. DO NOT EDIT.
//
// The ClaimValue value object.
//
// entity:     User
// spec:       specs/omnicore-gen/user.omnicore.yaml
// generator:  omnicore-gen
// generated:  2026-08-28
// checksum:   sha256:27535cd4ad0ecab40de67d0fbef36fde5dcde670ba700b335a8cbc512325c29e
//
// The checksum covers this file with the checksum line itself blanked. The
// generator recomputes it before every write: if it does not match, the file
// was changed by hand and the run REFUSES it — your change is neither
// overwritten nor updated. Change the spec and regenerate instead; to keep a
// deliberate edit, run omnicore-gen adopt on this path.

package vos

import "github.com/ClaudioSchirmer/omnicore/domain"

// ClaimValue is a string constrained by its own rule.
//
// A claim's value as a token would carry it: 1-256 runes, never empty. It
// carries no type rule of its own — what the value must PARSE as is declared
// on the claim definition, in another aggregate, and is enforced by a rule
// that can read it.
//
// The framework finds every field of this type and validates it on each write,
// so no rule elsewhere needs to repeat the check.
type ClaimValue string

// Value unwraps to the underlying type, which is what the wire and the
// database see.
func (v ClaimValue) Value() string { return string(v) }

// IsValid is the framework's entry point. It reports every problem it finds
// through the context rather than returning one, so a caller sees all of them
// at once.
func (v ClaimValue) IsValid(fieldName string, ctx *domain.NotificationContext) bool {
	if v == "" {
		ctx.AddNotification(fieldName, domain.RequiredFieldNotification{})
		return false
	}
	if len(v) < 1 || len(v) > 256 {
		ctx.AddNotification(fieldName, InvalidClaimValueNotification{}, v)
		return false
	}
	return true
}
