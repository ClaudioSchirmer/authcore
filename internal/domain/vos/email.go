// Code generated by omnicore-gen. DO NOT EDIT.
//
// The Email value object.
//
// entity:     User
// spec:       specs/omnicore-gen/user.omnicore.yaml
// generator:  omnicore-gen
// generated:  2026-08-29
// checksum:   sha256:ed12b9b24df6ee5c6a034b77a4b6a40936f1acf91be71a70fb6936778baa083d
//
// The line above is the Go convention that tells linters to skip this file.
// It is NOT a rule that the code may not change: this file is yours, in your
// repository, and improving it is ordinary work. What the generator asks is
// only that it be told.
//
// Changing the SPEC and regenerating is the cheaper of the two edits — it
// survives every later run. When the spec cannot say what you need and the
// framework can, edit this file: ask whoever owns the service first, and
// then run `omnicore-gen adopt <this path> -why '<what the spec could not
// express>'`. Adopting re-hashes the file as it now stands, so regeneration
// KEEPS your edit — and every later run prints the file as adopted, so it
// stays visible instead of becoming a surprise.
//
// Without that adopt, the next run notices the checksum no longer matches
// and REFUSES rather than overwriting — your work is never lost, but the
// run stops for whoever meets it.

package vos

import (
	"github.com/ClaudioSchirmer/omnicore/domain"
	"regexp"
)

// Compiled once at package level: compiling a pattern per call would pay for
// it on every write.
var emailPattern = regexp.MustCompile(`^[a-z0-9._%+\-]{1,64}@[a-z0-9]([a-z0-9\-]*[a-z0-9])?(\.[a-z0-9]([a-z0-9\-]*[a-z0-9])?)+$`)

// Email is a string constrained by its own rule.
//
// A lowercase e-mail address: a local part of at most 64 runes, an @, and a
// dotted domain. Uppercase is refused rather than folded, because the
// platform-wide uniqueness is over the stored value.
//
// The framework finds every field of this type and validates it on each write,
// so no rule elsewhere needs to repeat the check.
type Email string

// Value unwraps to the underlying type, which is what the wire and the
// database see.
func (v Email) Value() string { return string(v) }

// IsValid is the framework's entry point. It reports every problem it finds
// through the context rather than returning one, so a caller sees all of them
// at once.
func (v Email) IsValid(fieldName string, ctx *domain.NotificationContext) bool {
	if v == "" {
		ctx.AddNotification(fieldName, domain.RequiredFieldNotification{})
		return false
	}
	if len(v) > 254 {
		ctx.AddNotification(fieldName, InvalidEmailNotification{}, v)
		return false
	}
	if !emailPattern.MatchString(string(v)) {
		ctx.AddNotification(fieldName, InvalidEmailNotification{}, v)
		return false
	}
	return true
}
