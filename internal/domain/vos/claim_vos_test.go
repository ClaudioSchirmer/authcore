// Code generated by omnicore-gen. DO NOT EDIT.
//
// Tests for 2 value object(s).
//
// entity:     Claim
// spec:       specs/omnicore-gen/claim.omnicore.yaml
// generator:  omnicore-gen
// generated:  2026-08-28
// checksum:   sha256:8b52febf7c2111442affc616befea5307eeffe0b09c1140a31686db46bffdc6b
//
// The checksum covers this file with the checksum line itself blanked. The
// generator recomputes it before every write: if it does not match, the file
// was changed by hand and the run REFUSES it — your change is neither
// overwritten nor updated. Change the spec and regenerate instead; to keep a
// deliberate edit, run omnicore-gen adopt on this path.

package vos

import "testing"

// ClaimValueType declares its members; the framework validates membership
// against exactly this set.
func TestClaimValueTypeMembers(t *testing.T) {
	members := ClaimValueType("").Values()
	if len(members) != 3 {
		t.Fatalf("expected 3 members, got %d", len(members))
	}
	// The unknown sentinel must NOT be a member: it is where an out-of-set
	// value lands, so accepting it would defeat the whole check.
	for _, m := range members {
		if m == ClaimValueTypeUnknown {
			t.Error("the unknown sentinel must not be a declared member")
		}
	}
}

// A declared member of ClaimValueType is accepted and reads back unchanged.
//
// The pair matters: a value object that rejects everything passes the negative
// test below and is still broken.
// A value outside ClaimValueType's set is not one of its members.
//
// An enum declares no rule of its own: the framework decides membership from
// Values(), and answers with the unknown notification. So what is under test
// is the SET — that a member is in it and a stranger is not.
func TestClaimValueTypeMembership(t *testing.T) {
	in := func(v ClaimValueType) bool {
		for _, m := range v.Values() {
			if m == v {
				return true
			}
		}
		return false
	}
	if !in(ClaimValueTypeString) {
		t.Error("a declared member is not in its own set")
	}
	if in(ClaimValueType("!!not-valid!!")) {
		t.Error("a value outside the set was found in it")
	}
	if ClaimValueTypeString.Value() == "" {
		t.Error("a member reads back empty")
	}
	if ClaimValueTypeString.UnknownNotification() == nil {
		t.Error("the enum has no notification for an unknown value")
	}
}

// ClaimAppliesTo declares its members; the framework validates membership
// against exactly this set.
func TestClaimAppliesToMembers(t *testing.T) {
	members := ClaimAppliesTo("").Values()
	if len(members) != 3 {
		t.Fatalf("expected 3 members, got %d", len(members))
	}
	// The unknown sentinel must NOT be a member: it is where an out-of-set
	// value lands, so accepting it would defeat the whole check.
	for _, m := range members {
		if m == ClaimAppliesToUnknown {
			t.Error("the unknown sentinel must not be a declared member")
		}
	}
}

// A declared member of ClaimAppliesTo is accepted and reads back unchanged.
//
// The pair matters: a value object that rejects everything passes the negative
// test below and is still broken.
// A value outside ClaimAppliesTo's set is not one of its members.
//
// An enum declares no rule of its own: the framework decides membership from
// Values(), and answers with the unknown notification. So what is under test
// is the SET — that a member is in it and a stranger is not.
func TestClaimAppliesToMembership(t *testing.T) {
	in := func(v ClaimAppliesTo) bool {
		for _, m := range v.Values() {
			if m == v {
				return true
			}
		}
		return false
	}
	if !in(ClaimAppliesToUser) {
		t.Error("a declared member is not in its own set")
	}
	if in(ClaimAppliesTo("!!not-valid!!")) {
		t.Error("a value outside the set was found in it")
	}
	if ClaimAppliesToUser.Value() == "" {
		t.Error("a member reads back empty")
	}
	if ClaimAppliesToUser.UnknownNotification() == nil {
		t.Error("the enum has no notification for an unknown value")
	}
}
