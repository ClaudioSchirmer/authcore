// Code generated by omnicore-gen. DO NOT EDIT.
//
// Tests for the collection types.
//
// entity:     Role
// spec:       specs/omnicore-gen/role.omnicore.yaml
// generator:  omnicore-gen
// generated:  2026-08-21
// checksum:   sha256:198c1803968636a8de9a74014f09bded17f575447e8bae92797e514d87641c95
//
// The checksum covers this file with the checksum line itself blanked. The
// generator recomputes it before every write: if it does not match, the file
// was changed by hand and the run REFUSES it — your change is neither
// overwritten nor updated. Change the spec and regenerate instead; to keep a
// deliberate edit, run omnicore-gen adopt on this path.

package aggregatevos

import (
	"reflect"
	"testing"

	"github.com/ClaudioSchirmer/omnicore/domain"
)

// rulesForRolePermission opens a rule run for one entry.
//
// The gates fire as they are declared rather than on a later call, so the
// notifications are read from the context afterwards.
func rulesForRoleRolePermission() (*domain.NotificationContext, *domain.Rules) {
	ctx := domain.NewNotificationContext("RolePermission")
	return ctx, domain.NewRules(domain.ModeInsert, ctx, reflect.TypeOf(RolePermission{}))
}

// validRolePermission is one entry every rule accepts.
func validRoleRolePermission() RolePermission {
	return RolePermission{
		PermissionID: domain.NewID("9f14b0a2-6d38-4c5e-b7a1-2e0c5d81f4a3"),
	}
}

// RolePermission's collection name is a PERSISTED key, so it is pinned here.
//
// It is the segment the projection nests the collection under and the field a
// read DTO carries. Changing it is a data migration wearing a rename's
// clothes: the old documents keep the old key and nothing reads them back.
func TestRoleRolePermission_CollectionNameIsTheDocumentKey(t *testing.T) {
	if got := (RolePermission{}).CollectionName(); got != "Permissions" {
		t.Errorf("the collection is written under %q, and the documents already say %q", got, "Permissions")
	}
}

// Two RolePermission entries are the same one when their business identity
// matches.
//
// Sameness is the business identity, never the id: the same entry typed twice
// in one request carries two ids and is still one entry, and the aggregate
// refuses the duplicate on exactly this answer.
func TestRoleRolePermission_SamenessIsTheBusinessIdentity(t *testing.T) {
	a := validRoleRolePermission()
	b := validRoleRolePermission()
	if !a.IsSameBusinessIdentity(b) {
		t.Error("two entries with the same business identity were seen as different")
	}
	b.PermissionID = domain.NewID("00000000-0000-0000-0000-000000000002")
	if a.IsSameBusinessIdentity(b) {
		t.Errorf("changing PermissionID did not change the identity")
	}
}

// A well-formed RolePermission passes its own rules.
//
// The negative cases below are only meaningful if the positive one holds: a
// builder that never validates would make every rejection vacuous.
func TestRoleRolePermission_ValidPasses(t *testing.T) {
	ctx, r := rulesForRoleRolePermission()
	validRoleRolePermission().BuildRules("insert", nil, r)
	if msgs := ctx.Messages(); len(msgs) > 0 {
		t.Errorf("a valid RolePermission was refused: %v", msgs)
	}
}
