// Code generated by omnicore-gen. DO NOT EDIT.
//
// Tests for the client_allowed_cidr collection type.
//
// entity:     Client
// spec:       specs/omnicore-gen/client.omnicore.yaml
// generator:  omnicore-gen
// generated:  2026-09-01
// checksum:   sha256:103c1bebb5613747f32ec32f5e83878a63f1bf33e72469252d57dfb87abc04ce
//
// The line above is the Go convention that tells linters to skip this file.
// It is NOT a rule that the code may not change: this file is yours, in your
// repository, and improving it is ordinary work. What the generator asks is
// only that it be told.
//
// Changing the SPEC and regenerating is the cheaper of the two edits — it
// survives every later run. When the spec cannot say what you need and the
// framework can, edit this file: ask whoever owns the service first, and
// then run `omnicore-gen adopt <this path> -why '<what the spec could not
// express>'`. Adopting re-hashes the file as it now stands, so regeneration
// KEEPS your edit — and every later run prints the file as adopted, so it
// stays visible instead of becoming a surprise.
//
// Without that adopt, the next run notices the checksum no longer matches
// and REFUSES rather than overwriting — your work is never lost, but the
// run stops for whoever meets it.

package aggregatevos

import (
	"testing"

	"github.com/ClaudioSchirmer/authcore/internal/domain/vos"
	"github.com/ClaudioSchirmer/omnicore/domain"
)

// rulesForClientAllowedCIDR runs one entry through the framework's own seat.
//
// Not %s.BuildRules directly. A rule may end the validation pass — the
// `guard: true` barrier — and the framework unwinds that from inside the
// seat that invoked the rules; a body called by hand would let the unwind
// escape as a panic, and every test below would fail on a rule doing exactly
// what it was declared to do.
//
// The seat also validates the entry's value objects, which is what a write
// does, so what these tests see is what the service sees.
func rulesForClientClientAllowedCIDR(v ClientAllowedCIDR) *domain.NotificationContext {
	host := &clientRuleHost{}
	domain.ValidateAggregateChild(host, v, domain.ModeInsert, "insert", nil)
	return host.GetAggregateRoot().NotificationContext()
}

// validClientAllowedCIDR is one entry every rule accepts.
func validClientClientAllowedCIDR() ClientAllowedCIDR {
	return ClientAllowedCIDR{
		CIDR:  vos.CIDRBlock("203.0.113.0/24"),
		Label: vos.DisplayName("NAT gateway, sa-east-1"),
	}
}

// ClientAllowedCIDR's collection name is a PERSISTED key, so it is pinned
// here.
//
// It is the segment the projection nests the collection under and the field a
// read DTO carries. Changing it is a data migration wearing a rename's
// clothes: the old documents keep the old key and nothing reads them back.
func TestClientClientAllowedCIDR_CollectionNameIsTheDocumentKey(t *testing.T) {
	if got := (ClientAllowedCIDR{}).CollectionName(); got != "AllowedCIDRs" {
		t.Errorf("the collection is written under %q, and the documents already say %q", got, "AllowedCIDRs")
	}
}

// Two ClientAllowedCIDR entries are the same one when their business identity
// matches.
//
// Sameness is the business identity, never the id: the same entry typed twice
// in one request carries two ids and is still one entry, and the aggregate
// refuses the duplicate on exactly this answer.
func TestClientClientAllowedCIDR_SamenessIsTheBusinessIdentity(t *testing.T) {
	a := validClientClientAllowedCIDR()
	b := validClientClientAllowedCIDR()
	if !a.IsSameBusinessIdentity(b) {
		t.Error("two entries with the same business identity were seen as different")
	}
	b.CIDR = vos.CIDRBlock("changed-value")
	if a.IsSameBusinessIdentity(b) {
		t.Errorf("changing CIDR did not change the identity")
	}
}

// A well-formed ClientAllowedCIDR passes its own rules.
//
// The negative cases below are only meaningful if the positive one holds: a
// builder that never validates would make every rejection vacuous.
func TestClientClientAllowedCIDR_ValidPasses(t *testing.T) {
	ctx := rulesForClientClientAllowedCIDR(validClientClientAllowedCIDR())
	if msgs := ctx.Messages(); len(msgs) > 0 {
		t.Errorf("a valid ClientAllowedCIDR was refused: %v", msgs)
	}
}
