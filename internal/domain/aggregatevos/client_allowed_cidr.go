// Code generated by omnicore-gen. DO NOT EDIT.
//
// The ClientAllowedCIDR child value object.
//
// entity:     Client
// spec:       specs/omnicore-gen/client.omnicore.yaml
// generator:  omnicore-gen
// generated:  2026-08-26
// checksum:   sha256:0ee9a203642508a9e7dfd521eb948c067b11b501d544a789ecbad7b9b5c1c93e
//
// The checksum covers this file with the checksum line itself blanked. The
// generator recomputes it before every write: if it does not match, the file
// was changed by hand and the run REFUSES it — your change is neither
// overwritten nor updated. Change the spec and regenerate instead; to keep a
// deliberate edit, run omnicore-gen adopt on this path.

package aggregatevos

import (
	"github.com/ClaudioSchirmer/authcore/internal/domain/vos"
	"github.com/ClaudioSchirmer/omnicore/domain"
)

// ClientAllowedCIDR is one entry of Client's allowedCIDRs collection.
//
// The network ranges this client may authenticate from. An EMPTY collection
// means any address: the restriction is opt-in, so a newly created client is
// not born unable to sign in.
//
// It embeds the framework's managed carrier and declares NO id field of its
// own: an exported ID here would compile, never be persisted, and never come
// back on a read.
type ClientAllowedCIDR struct {
	domain.Managed
	CIDR  vos.CIDRBlock   `labelKey:"ClientAllowedCIDRCIDRField"`  // A network range this client may authenticate from, stored normalised
	Label vos.DisplayName `labelKey:"ClientAllowedCIDRLabelField"` // What this range is, in the tenant's words — an unlabelled range is one nobody dares remove
}

// CollectionName is the name of the allowedCIDRs collection.
//
// It is a constant of the TYPE, resolved once from a zero value, and it is the
// single source for the document segment, the read DTO's field and the
// notification path — so the three can never drift apart.
func (ClientAllowedCIDR) CollectionName() string { return "AllowedCIDRs" }

// IsSameBusinessIdentity decides whether two entries are the same one.
//
// The framework matches children through this, never by comparing every field.
// It answers from the BUSINESS view, so editing a detail still leaves the
// entry recognisable and it keeps its id instead of being replaced.
func (c ClientAllowedCIDR) IsSameBusinessIdentity(other domain.AggregateValueObject) bool {
	o, ok := other.(ClientAllowedCIDR)
	if !ok {
		return false
	}
	return c.CIDR == o.CIDR
}

// BuildRules validates one ClientAllowedCIDR.
//
// It runs scoped to this entry, so a notification it raises reaches the caller
// addressed to the exact position in the collection rather than to the root.
func (c ClientAllowedCIDR) BuildRules(actionName string, service domain.Service, r *domain.Rules) {
	// No rule beyond what the value objects validate on their own.
}
