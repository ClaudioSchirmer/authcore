// Code generated by omnicore-gen. DO NOT EDIT.
//
// The UserGroup child value object.
//
// entity:     User
// spec:       specs/omnicore-gen/user.omnicore.yaml
// generator:  omnicore-gen
// generated:  2026-08-29
// checksum:   sha256:01eaa07511f092fee9bb22887165f72a8e34275f8a857f16407eef0029eff8e1
//
// The line above is the Go convention that tells linters to skip this file.
// It is NOT a rule that the code may not change: this file is yours, in your
// repository, and improving it is ordinary work. What the generator asks is
// only that it be told.
//
// Changing the SPEC and regenerating is the cheaper of the two edits — it
// survives every later run. When the spec cannot say what you need and the
// framework can, edit this file: ask whoever owns the service first, and
// then run `omnicore-gen adopt <this path> -why '<what the spec could not
// express>'`. Adopting re-hashes the file as it now stands, so regeneration
// KEEPS your edit — and every later run prints the file as adopted, so it
// stays visible instead of becoming a surprise.
//
// Without that adopt, the next run notices the checksum no longer matches
// and REFUSES rather than overwriting — your work is never lost, but the
// run stops for whoever meets it.

package aggregatevos

import "github.com/ClaudioSchirmer/omnicore/domain"

// UserGroup is one entry of User's groups collection.
//
// The groups this user belongs to. One row per membership, holding nothing but
// the group's id — so a retired-and-recreated group is never silently
// re-joined.
//
// It embeds the framework's managed carrier and declares NO id field of its
// own: an exported ID here would compile, never be persisted, and never come
// back on a read.
type UserGroup struct {
	domain.Managed
	GroupID domain.ID `labelKey:"UserGroupGroupIDField"` // The group this membership joins — the id and not the key, so a retired-and-recreated group needs an explicit re-join

	// Filled by the READ JOINS the repository declares — see WithJoins in
	// internal/infra. They are ordinary fields of this entry, populated on EVERY
	// load, and readable by the rules like any other; they are absent from the
	// TableSchema, so no write can carry them and no migration creates them.
	GroupKey  string // The group's stable machine handle. Read-only, filled on load — Group.group_key, via the InnerJoin on group_id
	GroupName string // The group's display name. Read-only, filled on load — Group.name, via the InnerJoin on group_id
}

// CollectionName is the name of the groups collection.
//
// It is a constant of the TYPE, resolved once from a zero value, and it is the
// single source for the document segment, the read DTO's field and the
// notification path — so the three can never drift apart.
func (UserGroup) CollectionName() string { return "Groups" }

// IsSameBusinessIdentity decides whether two entries are the same one.
//
// The framework matches children through this, never by comparing every field.
// It answers from the BUSINESS view, so editing a detail still leaves the
// entry recognisable and it keeps its id instead of being replaced.
func (c UserGroup) IsSameBusinessIdentity(other domain.AggregateValueObject) bool {
	o, ok := other.(UserGroup)
	if !ok {
		return false
	}
	return c.GroupID == o.GroupID
}

// BuildRules validates one UserGroup.
//
// It runs scoped to this entry, so a notification it raises reaches the caller
// addressed to the exact position in the collection rather than to the root.
func (c UserGroup) BuildRules(actionName string, service domain.Service, r *domain.Rules) {
	// No rule beyond what the value objects validate on their own.
}
