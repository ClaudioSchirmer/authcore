// Code generated by omnicore-gen. DO NOT EDIT.
//
// The Claim service port (4 fact(s)).
//
// entity:     Claim
// spec:       specs/omnicore-gen/claim.omnicore.yaml
// generator:  omnicore-gen
// generated:  2026-08-28
// checksum:   sha256:d4d496303e7cbe9a9ad30e7ad517f26f50a0782cee936243646a16ed7ea5a5f8
//
// The checksum covers this file with the checksum line itself blanked. The
// generator recomputes it before every write: if it does not match, the file
// was changed by hand and the run REFUSES it — your change is neither
// overwritten nor updated. Change the spec and regenerate instead; to keep a
// deliberate edit, run omnicore-gen adopt on this path.

package domain

import "github.com/ClaudioSchirmer/omnicore/domain"

// ClaimService answers the questions Claim's rules cannot answer alone.
//
// Every method returns a plain value and NO error. That is deliberate: the
// domain performs no IO, so an error here would leave a rule with three bad
// options — panic, swallow it, or invent an answer. A failed query is
// infra's problem and is raised there.
type ClaimService interface {
	domain.Service

	// Reports whether another ACTIVE claim in the same tenant already holds
	// this name. Excludes the row being updated.
	ClaimNameTaken(tenantID domain.ID, name string, selfID domain.ID) bool

	// Whether the owning tenant is missing, archived, or commercially
	// SUSPENDED. Queries the tenants table by its primary key. A trial tenant
	// is a live customer and is available.
	TenantIsUnavailable(tenantID domain.ID) bool

	// Whether any ACTIVE user_claims row references the ACTIVE claim
	// definition identified by this tenant and name. Join user_claims to
	// claims on claim_id and require user_claims.deleted_at IS NULL AND
	// claims.deleted_at IS NULL — archived edges do not count, because a
	// value somebody removed must not freeze the definition's shape, and the
	// archived-definition half is what keeps a retired row's leftovers out of
	// the answer.
	ClaimIsHeldByAUser(tenantID domain.ID, name string) bool

	// Whether any ACTIVE client_claims row references the ACTIVE claim
	// definition identified by this tenant and name. Same join and the same
	// two archive predicates as its user twin.
	ClaimIsHeldByAClient(tenantID domain.ID, name string) bool
}
