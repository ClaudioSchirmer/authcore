// Code generated by omnicore-gen. DO NOT EDIT.
//
// The shapes for 3 child collection(s).
//
// entity:     User
// spec:       specs/omnicore-gen/user.omnicore.yaml
// generator:  omnicore-gen
// generated:  2026-08-28
// checksum:   sha256:c68544d9311a371a1122a06e3c89345da70d9515272400784603039b671de2bc
//
// The checksum covers this file with the checksum line itself blanked. The
// generator recomputes it before every write: if it does not match, the file
// was changed by hand and the run REFUSES it — your change is neither
// overwritten nor updated. Change the spec and regenerate instead; to keep a
// deliberate edit, run omnicore-gen adopt on this path.

package commands

import (
	appdomain "github.com/ClaudioSchirmer/authcore/internal/domain"
	"github.com/ClaudioSchirmer/authcore/internal/domain/aggregatevos"
	"github.com/ClaudioSchirmer/omnicore/domain"
)

// UserGroupResult mirrors one persisted UserGroup.
//
// The id is included because the persister writes the minted ids back before
// this projection runs, and the caller needs them to address the entry later.
type UserGroupResult struct {
	ID      domain.ID
	GroupID domain.ID
}

// UserRoleResult mirrors one persisted UserRole.
//
// The id is included because the persister writes the minted ids back before
// this projection runs, and the caller needs them to address the entry later.
type UserRoleResult struct {
	ID     domain.ID
	RoleID domain.ID
}

// UserClaimResult mirrors one persisted UserClaim.
//
// The id is included because the persister writes the minted ids back before
// this projection runs, and the caller needs them to address the entry later.
type UserClaimResult struct {
	ID      domain.ID
	ClaimID domain.ID
	Value   string
}

func projectUserGroups(e *appdomain.User) []UserGroupResult {
	items := domain.GetCurrentItemsOf[aggregatevos.UserGroup](&e.AggregateRoot)
	out := make([]UserGroupResult, 0, len(items))
	for _, item := range items {
		out = append(out, UserGroupResult{
			ID:      item.GetID(),
			GroupID: item.GroupID,
		})
	}
	return out
}

func projectUserRoles(e *appdomain.User) []UserRoleResult {
	items := domain.GetCurrentItemsOf[aggregatevos.UserRole](&e.AggregateRoot)
	out := make([]UserRoleResult, 0, len(items))
	for _, item := range items {
		out = append(out, UserRoleResult{
			ID:     item.GetID(),
			RoleID: item.RoleID,
		})
	}
	return out
}

func projectUserClaims(e *appdomain.User) []UserClaimResult {
	items := domain.GetCurrentItemsOf[aggregatevos.UserClaim](&e.AggregateRoot)
	out := make([]UserClaimResult, 0, len(items))
	for _, item := range items {
		out = append(out, UserClaimResult{
			ID:      item.GetID(),
			ClaimID: item.ClaimID,
			Value:   item.Value.Value(),
		})
	}
	return out
}
