// Code generated by omnicore-gen. DO NOT EDIT.
//
// The insert command and result.
//
// entity:     Permission
// spec:       specs/omnicore-gen/permission.omnicore.yaml
// generator:  omnicore-gen
// generated:  2026-08-24
// checksum:   sha256:2d0238f2ace01fed972d9ccc1666920a2e237b41f317913af2fb1e40e95049fe
//
// The checksum covers this file with the checksum line itself blanked. The
// generator recomputes it before every write: if it does not match, the file
// was changed by hand and the run REFUSES it — your change is neither
// overwritten nor updated. Change the spec and regenerate instead; to keep a
// deliberate edit, run omnicore-gen adopt on this path.

package commands

import (
	"github.com/ClaudioSchirmer/omnicore/application/configuration"
	"github.com/ClaudioSchirmer/omnicore/application/pipeline"
	"github.com/ClaudioSchirmer/omnicore/domain"

	appdomain "github.com/ClaudioSchirmer/authcore/internal/domain"
	"github.com/ClaudioSchirmer/authcore/internal/domain/vos"

	appqueries "github.com/ClaudioSchirmer/authcore/internal/application/queries"
)

// InsertPermissionCommand carries the writable fields of the request.
type InsertPermissionCommand struct {
	pipeline.CommandWithBodyBase
	Resource    string
	Action      string
	Description string
}

// ToEntity builds the aggregate the framework will validate and persist.
func (c *InsertPermissionCommand) ToEntity(ctx *configuration.AppContext) (*appdomain.Permission, error) {
	e := &appdomain.Permission{}
	e.Description = vos.Description(c.Description)
	e.Key = vos.PermissionKey{
		Resource: c.Resource,
		Action:   c.Action,
	}
	return e, nil
}

// InsertPermissionResult is the write response, projected from the entity.
type InsertPermissionResult struct {
	ID          domain.ID
	Resource    string
	Action      string
	Description string
	// Permission is COMPUTED: no column backs it, and FromEntity fills it
	// from Resource+Action.
	Permission string
}

// FromEntity projects the aggregate AFTER it was validated and written.
//
// It reads the entity, never the command: the domain may have normalised or
// defaulted a value, and echoing the input back would hide that from the
// caller.
//
// The computed fields are derived through the SAME function the reads call, in
// internal/application/queries. Deriving them a second time here is how one
// question grows two answers.
func (c *InsertPermissionCommand) FromEntity(ctx *configuration.AppContext, e *appdomain.Permission) (InsertPermissionResult, error) {
	out := InsertPermissionResult{
		ID:          *e.GetID(),
		Description: e.Description.Value(),
	}
	out.Resource = e.Key.Resource
	out.Action = e.Key.Action
	{
		v, err := appqueries.ComputePermissionPermission(ctx, e.Key.Resource, e.Key.Action)
		if err != nil {
			return out, err
		}
		out.Permission = v
	}
	return out, nil
}
