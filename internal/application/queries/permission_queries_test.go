// Code generated by omnicore-gen. DO NOT EDIT.
//
// The read criteria tests.
//
// entity:     Permission
// spec:       specs/omnicore-gen/permission.omnicore.yaml
// generator:  omnicore-gen
// generated:  2026-08-24
// checksum:   sha256:eb99dd589417212c27a08c71bf146e406be9841053abd5ee8b64f1817fa0782e
//
// The checksum covers this file with the checksum line itself blanked. The
// generator recomputes it before every write: if it does not match, the file
// was changed by hand and the run REFUSES it — your change is neither
// overwritten nor updated. Change the spec and regenerate instead; to keep a
// deliberate edit, run omnicore-gen adopt on this path.

package queries

import (
	"testing"

	"github.com/ClaudioSchirmer/omnicore/application/configuration"
)

// The criteria mappers must not fail on a well-formed query.
//
// A mapper that errors here turns a legitimate read into a 400, and a mapper
// that silently drops a restriction turns it into someone else's rows — so
// the mapping is exercised rather than assumed.
func TestPermissionCriteriaBuild(t *testing.T) {
	// No identity attached: the anonymous path, which is the one that
	// applies every restriction rather than skipping them.
	ctx := &configuration.AppContext{}
	if _, err := (FindPermissionByIDQuery{}).ToCriteria(ctx); err != nil {
		t.Errorf("the by-id criteria failed: %v", err)
	}
	if _, err := (FindPermissionsByParamsQuery{}).ToCriteria(ctx); err != nil {
		t.Errorf("the listing criteria failed: %v", err)
	}
}

// The derivation seat must survive a Result with nothing in it.
//
// FromQueryResult runs once per document on every read, so an error out of it
// is not a missing field — it is the endpoint answering 500. An empty Result
// is the real case, not a contrived one: a ?fields= selection that named none
// of a computed field's sources produces exactly this.
func TestPermissionReadsSurviveAnEmptyResult(t *testing.T) {
	ctx := &configuration.AppContext{}
	if _, err := (FindPermissionByIDQuery{}).FromQueryResult(ctx, FindPermissionByIDResult{}); err != nil {
		t.Errorf("the by-id derivation failed: %v", err)
	}
	if _, err := (FindPermissionsByParamsQuery{}).FromQueryResult(ctx, FindPermissionsByParamsResult{}); err != nil {
		t.Errorf("the listing derivation failed: %v", err)
	}
}

// The derivations RUN when the sources they declared are there.
//
// The bodies are hand-written and may legitimately return nothing yet; what is
// under test is the seat around them — that the generated unwrapping, the
// call and the assignment hold together for a Result that carries every
// source.
func TestPermissionDerivationsRun(t *testing.T) {
	ctx := &configuration.AppContext{}
	{
		r := FindPermissionByIDResult{Resource: "tenant", Action: "read"}
		if _, err := (FindPermissionByIDQuery{}).FromQueryResult(ctx, r); err != nil {
			t.Errorf("the by-id derivation failed on a filled result: %v", err)
		}
	}
	{
		resource := string("tenant")
		action := string("read")
		r := FindPermissionsByParamsResult{Resource: &resource, Action: &action}
		if _, err := (FindPermissionsByParamsQuery{}).FromQueryResult(ctx, r); err != nil {
			t.Errorf("the listing derivation failed on a filled result: %v", err)
		}
	}
}

// Both queries report the same context name.
//
// It is the name every notification of this read is grouped under, so two
// spellings split one entity's errors into two piles for whoever reads them.
func TestPermissionQueriesShareAContext(t *testing.T) {
	for _, got := range []string{
		FindPermissionByIDQuery{}.ContextName(),
		FindPermissionsByParamsQuery{}.ContextName(),
	} {
		if got != "Permission" {
			t.Errorf("a query reports the context %q, the entity is %q", got, "Permission")
		}
	}
}
