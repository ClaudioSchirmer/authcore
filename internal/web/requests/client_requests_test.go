// Code generated by omnicore-gen. DO NOT EDIT.
//
// The request mapper tests.
//
// entity:     Client
// spec:       specs/omnicore-gen/client.omnicore.yaml
// generator:  omnicore-gen
// generated:  2026-08-28
// checksum:   sha256:555563616be1afd82c8b0967d52dc235ddacee9f139e17764b7e31fdff608965
//
// The checksum covers this file with the checksum line itself blanked. The
// generator recomputes it before every write: if it does not match, the file
// was changed by hand and the run REFUSES it — your change is neither
// overwritten nor updated. Change the spec and regenerate instead; to keep a
// deliberate edit, run omnicore-gen adopt on this path.

package requests

import (
	"testing"

	"github.com/ClaudioSchirmer/authcore/internal/application/commands"
	appqueries "github.com/ClaudioSchirmer/authcore/internal/application/queries"
	fwqueries "github.com/ClaudioSchirmer/omnicore/application/queries"
	fwresults "github.com/ClaudioSchirmer/omnicore/application/results"
	"github.com/ClaudioSchirmer/omnicore/domain"
)

// InsertClientRequest must carry every field into the command.
//
// A field dropped here is invisible: the request parses, the write succeeds,
// and the value the caller sent is not in the row.
func TestInsertClientRequest_CarriesEveryField(t *testing.T) {
	r := InsertClientRequest{
		Name:        "Billing integration",
		Description: "Posts invoices from the billing system into the ledger.",
		Status:      "active",
		TenantID:    func() *domain.ID { v := domain.ID(domain.NewID("0198f3c2-6b41-7c9e-9f2a-6d3b1e77a410")); return &v }(),
	}
	r.Roles = []ClientRoleRequest{{
		RoleID: domain.NewID("0198f3e0-1a44-7bb2-9c31-77c0d5e1b904"),
	}}
	r.AllowedCIDRs = []ClientAllowedCIDRRequest{{
		CIDR:  "203.0.113.0/24",
		Label: "NAT gateway, sa-east-1",
	}}
	r.Claims = []ClientClaimRequest{{
		ClaimID: domain.NewID("0198f3e0-7b31-7c02-8a55-1f9d2e6b4c17"),
		Value:   "sa-east-1",
	}}
	cmd := r.ToCommand()
	if cmd == nil {
		t.Fatal("the mapper produced no command")
	}
	if len(cmd.Roles) != 1 {
		t.Errorf("the ClientRole collection did not reach the command")
	}
	if len(cmd.AllowedCIDRs) != 1 {
		t.Errorf("the ClientAllowedCIDR collection did not reach the command")
	}
	if len(cmd.Claims) != 1 {
		t.Errorf("the ClientClaim collection did not reach the command")
	}
	if cmd.Name != r.Name {
		t.Errorf("Name did not reach the command")
	}
	if cmd.Description != r.Description {
		t.Errorf("Description did not reach the command")
	}
	if cmd.Status != r.Status {
		t.Errorf("Status did not reach the command")
	}
}

// InsertClientResponse must carry the result back out.
//
// The response is the caller's only view of what was written, so a field lost
// here reads as a field that was never saved.
func TestInsertClientResponse_CarriesTheResult(t *testing.T) {
	var in commands.InsertClientResult
	_ = InsertClientResponse{}.FromResult(in)
}

// PatchClientRequest must carry every field into the command.
//
// A field dropped here is invisible: the request parses, the write succeeds,
// and the value the caller sent is not in the row.
func TestPatchClientRequest_CarriesEveryField(t *testing.T) {
	r := PatchClientRequest{
		Name:        func() *string { v := string("Billing integration"); return &v }(),
		Description: func() *string { v := string("Posts invoices from the billing system into the ledger."); return &v }(),
		Status:      func() *string { v := string("active"); return &v }(),
	}
	cmd := r.ToCommand()
	if cmd == nil {
		t.Fatal("the mapper produced no command")
	}
}

// PatchClientResponse must carry the result back out.
//
// The response is the caller's only view of what was written, so a field lost
// here reads as a field that was never saved.
func TestPatchClientResponse_CarriesTheResult(t *testing.T) {
	var in commands.PatchClientResult
	_ = PatchClientResponse{}.FromResult(in)
}

// The by-id request maps onto the query it stands for.
//
// It carries the controls the spec declared for a single read; one dropped
// here is a parameter the caller sends and the read ignores.
func TestFindClientByIDRequestBuildsItsQuery(t *testing.T) {
	if q := (FindClientByIDRequest{}).ToQuery(fwqueries.ReadCriteria{}); q == nil {
		t.Fatal("the by-id request produced no query")
	}
	// The criteria travels WHOLE. A by-id read takes it the same way the
	// paged one does — the wrapper parses the wire, the mapper carries it
	// through — so a mapper that rebuilt a fresh criteria instead would
	// drop whatever the wire said, silently and on every request.
	q := (FindClientByIDRequest{}).ToQuery(fwqueries.ReadCriteria{IncludeArchived: true})
	if q == nil || !q.Criteria.IncludeArchived {
		t.Error("the parsed criteria did not reach the query")
	}
}

// The listing request maps onto the query it stands for.
//
// Every control it carries is one the spec declared; one dropped here is a
// parameter the caller sends and the read silently ignores.
func TestFindClientsRequestBuildsItsQuery(t *testing.T) {
	q := FindClientsRequest{}.ToQuery(fwqueries.ReadCriteria{})
	if q == nil {
		t.Fatal("the listing request produced no query")
	}
}

// FindClientByIDResponse is filled from FindClientByIDResult and from nothing
// else.
//
// The mapping is name-based, so a rename on one side and not the other
// produces a field that is silently always empty rather than a compile error.
// The framework catches it at mount; this catches it here, where it is cheap.
func TestFindClientByIDResponse_IsFilledFromTheResult(t *testing.T) {
	out := FindClientByIDResponse{}.FromResult(appqueries.FindClientByIDResult{ID: "7b3c1f10-3c7e-4a8d-9f0e-9d2a8e6d4b51"})
	if out.ID != "7b3c1f10-3c7e-4a8d-9f0e-9d2a8e6d4b51" {
		t.Fatalf("the id did not survive the mapping: %q", out.ID)
	}
}

// FindClientsResponse is filled from FindClientsByParamsResult and from
// nothing else.
//
// The mapping is name-based, so a rename on one side and not the other
// produces a field that is silently always empty rather than a compile error.
// The framework catches it at mount; this catches it here, where it is cheap.
func TestFindClientsResponse_IsFilledFromTheResult(t *testing.T) {
	id := "7b3c1f10-3c7e-4a8d-9f0e-9d2a8e6d4b51"
	out := FindClientsResponse{}.FromResult(appqueries.FindClientsByParamsResult{ID: &id})
	if out.ID == nil || *out.ID != id {
		t.Fatalf("the id did not survive the mapping: %v", out.ID)
	}
}

// AddClientRoleRequest carries the entry into its command.
//
// The body is the same entry shape the root's own body carries, so a field
// forgotten here is saved as missing on a request that answered 201.
func TestAddClientRoleRequest_CarriesEveryField(t *testing.T) {
	r := AddClientRoleRequest{ClientRoleRequest: ClientRoleRequest{
		RoleID: domain.NewID("0198f3e0-1a44-7bb2-9c31-77c0d5e1b904"),
	}}
	cmd := r.ToCommand()
	if cmd.RoleID != domain.NewID("0198f3e0-1a44-7bb2-9c31-77c0d5e1b904") {
		t.Errorf("RoleID did not reach the command")
	}
}

// The ClientRole responses carry the stored entry back to the caller.
//
// The entry comes back with the id the SERVER minted, which is how the caller
// addresses it afterwards — a response mapper that drops it answers 201 with
// nothing to act on.
func TestAddClientRoleResponse_CarriesTheStoredEntry(t *testing.T) {
	ownerID := domain.NewRandomID()
	entryID := domain.NewRandomID()
	res := AddClientRoleResponse{}.FromResult(commands.AddClientRoleResult{
		ClientID: ownerID,
		ClientRole: commands.ClientRoleResult{ID: entryID,
			RoleID: domain.NewID("0198f3e0-1a44-7bb2-9c31-77c0d5e1b904"),
		},
	})
	if res.ClientID != ownerID {
		t.Error("the owner id did not reach the response")
	}
	if res.ClientRole.ID != entryID {
		t.Error("the entry id the server minted did not reach the response")
	}
	if res.ClientRole.RoleID != domain.NewID("0198f3e0-1a44-7bb2-9c31-77c0d5e1b904") {
		t.Errorf("RoleID did not reach the response")
	}
}

// RemoveClientRoleRequest carries the addressed entry into its command.
func TestRemoveClientRoleRequest_NamesTheEntry(t *testing.T) {
	r := RemoveClientRoleRequest{ClientRoleID: "01890000-0000-7000-8000-000000000000"}
	if r.ToCommand().ClientRoleID != "01890000-0000-7000-8000-000000000000" {
		t.Error("the entry id did not reach the command, so the wrong entry would be removed")
	}
}

// RemoveClientRoleGraphQLRequest names the entry through its input.
func TestRemoveClientRoleGraphQLRequest_NamesTheEntry(t *testing.T) {
	r := RemoveClientRoleGraphQLRequest{ClientRoleID: "01890000-0000-7000-8000-000000000000"}
	if r.ToCommand().ClientRoleID != "01890000-0000-7000-8000-000000000000" {
		t.Error("the entry id did not reach the command, so the wrong entry would be removed")
	}
}

// RemoveClientRoleGraphQLResponse acknowledges, since a mutation must answer
// something.
//
// Its REST twin answers 204 with no body at all. This one is the only
// projection in the collection's wire types that is not the generic mapper,
// which is exactly why it is asserted rather than assumed.
func TestRemoveClientRoleGraphQLResponse_Acknowledges(t *testing.T) {
	if !(RemoveClientRoleGraphQLResponse{}).FromResult(fwresults.None{}).Success {
		t.Error("a successful removal answered success: false")
	}
}

// AddClientAllowedCIDRRequest carries the entry into its command.
//
// The body is the same entry shape the root's own body carries, so a field
// forgotten here is saved as missing on a request that answered 201.
func TestAddClientAllowedCIDRRequest_CarriesEveryField(t *testing.T) {
	r := AddClientAllowedCIDRRequest{ClientAllowedCIDRRequest: ClientAllowedCIDRRequest{
		CIDR:  "203.0.113.0/24",
		Label: "NAT gateway, sa-east-1",
	}}
	cmd := r.ToCommand()
	if cmd.CIDR != "203.0.113.0/24" {
		t.Errorf("CIDR did not reach the command")
	}
	if cmd.Label != "NAT gateway, sa-east-1" {
		t.Errorf("Label did not reach the command")
	}
}

// The ClientAllowedCIDR responses carry the stored entry back to the caller.
//
// The entry comes back with the id the SERVER minted, which is how the caller
// addresses it afterwards — a response mapper that drops it answers 201 with
// nothing to act on.
func TestAddClientAllowedCIDRResponse_CarriesTheStoredEntry(t *testing.T) {
	ownerID := domain.NewRandomID()
	entryID := domain.NewRandomID()
	res := AddClientAllowedCIDRResponse{}.FromResult(commands.AddClientAllowedCIDRResult{
		ClientID: ownerID,
		ClientAllowedCIDR: commands.ClientAllowedCIDRResult{ID: entryID,
			CIDR:  "203.0.113.0/24",
			Label: "NAT gateway, sa-east-1",
		},
	})
	if res.ClientID != ownerID {
		t.Error("the owner id did not reach the response")
	}
	if res.ClientAllowedCIDR.ID != entryID {
		t.Error("the entry id the server minted did not reach the response")
	}
	if res.ClientAllowedCIDR.CIDR != "203.0.113.0/24" {
		t.Errorf("CIDR did not reach the response")
	}
	if res.ClientAllowedCIDR.Label != "NAT gateway, sa-east-1" {
		t.Errorf("Label did not reach the response")
	}
}

// RemoveClientAllowedCIDRRequest carries the addressed entry into its command.
func TestRemoveClientAllowedCIDRRequest_NamesTheEntry(t *testing.T) {
	r := RemoveClientAllowedCIDRRequest{ClientAllowedCIDRID: "01890000-0000-7000-8000-000000000000"}
	if r.ToCommand().ClientAllowedCIDRID != "01890000-0000-7000-8000-000000000000" {
		t.Error("the entry id did not reach the command, so the wrong entry would be removed")
	}
}

// RemoveClientAllowedCIDRGraphQLRequest names the entry through its input.
func TestRemoveClientAllowedCIDRGraphQLRequest_NamesTheEntry(t *testing.T) {
	r := RemoveClientAllowedCIDRGraphQLRequest{ClientAllowedCIDRID: "01890000-0000-7000-8000-000000000000"}
	if r.ToCommand().ClientAllowedCIDRID != "01890000-0000-7000-8000-000000000000" {
		t.Error("the entry id did not reach the command, so the wrong entry would be removed")
	}
}

// RemoveClientAllowedCIDRGraphQLResponse acknowledges, since a mutation must
// answer something.
//
// Its REST twin answers 204 with no body at all. This one is the only
// projection in the collection's wire types that is not the generic mapper,
// which is exactly why it is asserted rather than assumed.
func TestRemoveClientAllowedCIDRGraphQLResponse_Acknowledges(t *testing.T) {
	if !(RemoveClientAllowedCIDRGraphQLResponse{}).FromResult(fwresults.None{}).Success {
		t.Error("a successful removal answered success: false")
	}
}

// AddClientClaimRequest carries the entry into its command.
//
// The body is the same entry shape the root's own body carries, so a field
// forgotten here is saved as missing on a request that answered 201.
func TestAddClientClaimRequest_CarriesEveryField(t *testing.T) {
	r := AddClientClaimRequest{ClientClaimRequest: ClientClaimRequest{
		ClaimID: domain.NewID("0198f3e0-7b31-7c02-8a55-1f9d2e6b4c17"),
		Value:   "sa-east-1",
	}}
	cmd := r.ToCommand()
	if cmd.ClaimID != domain.NewID("0198f3e0-7b31-7c02-8a55-1f9d2e6b4c17") {
		t.Errorf("ClaimID did not reach the command")
	}
	if cmd.Value != "sa-east-1" {
		t.Errorf("Value did not reach the command")
	}
}

// PatchClientClaimRequest names the entry AND carries what may change.
//
// The id comes from the path and the body is partial, so an id that does not
// reach the command patches the wrong entry, or none.
func TestPatchClientClaimRequest_CarriesTheEntryAndItsID(t *testing.T) {
	r := PatchClientClaimRequest{ClientClaimID: "01890000-0000-7000-8000-000000000000",
		Value: func() *string { v := string("sa-east-1"); return &v }(),
	}
	cmd := r.ToCommand()
	if cmd.ClientClaimID != "01890000-0000-7000-8000-000000000000" {
		t.Error("the entry id did not reach the command, so the wrong entry would be patched")
	}
	if cmd.Value == nil || *cmd.Value != "sa-east-1" {
		t.Errorf("Value did not reach the command")
	}
}

// The ClientClaim responses carry the stored entry back to the caller.
//
// The entry comes back with the id the SERVER minted, which is how the caller
// addresses it afterwards — a response mapper that drops it answers 201 with
// nothing to act on.
func TestAddClientClaimResponse_CarriesTheStoredEntry(t *testing.T) {
	ownerID := domain.NewRandomID()
	entryID := domain.NewRandomID()
	res := AddClientClaimResponse{}.FromResult(commands.AddClientClaimResult{
		ClientID: ownerID,
		ClientClaim: commands.ClientClaimResult{ID: entryID,
			ClaimID: domain.NewID("0198f3e0-7b31-7c02-8a55-1f9d2e6b4c17"),
			Value:   "sa-east-1",
		},
	})
	if res.ClientID != ownerID {
		t.Error("the owner id did not reach the response")
	}
	if res.ClientClaim.ID != entryID {
		t.Error("the entry id the server minted did not reach the response")
	}
	if res.ClientClaim.ClaimID != domain.NewID("0198f3e0-7b31-7c02-8a55-1f9d2e6b4c17") {
		t.Errorf("ClaimID did not reach the response")
	}
	if res.ClientClaim.Value != "sa-east-1" {
		t.Errorf("Value did not reach the response")
	}
}

// The PatchClientClaim response carries the entry back WHOLE, with the id it
// keeps.
//
// A partial change answers with the entry as stored, which is how the caller
// sees what the fields they did not send actually hold.
func TestPatchClientClaimResponse_CarriesTheStoredEntry(t *testing.T) {
	ownerID := domain.NewRandomID()
	entryID := domain.NewRandomID()
	res := PatchClientClaimResponse{}.FromResult(commands.PatchClientClaimResult{
		ClientID: ownerID,
		ClientClaim: commands.ClientClaimResult{ID: entryID,
			ClaimID: domain.NewID("0198f3e0-7b31-7c02-8a55-1f9d2e6b4c17"),
			Value:   "sa-east-1",
		},
	})
	if res.ClientID != ownerID {
		t.Error("the owner id did not reach the response")
	}
	if res.ClientClaim.ID != entryID {
		t.Error("the entry kept its id and the response did not carry it")
	}
	if res.ClientClaim.ClaimID != domain.NewID("0198f3e0-7b31-7c02-8a55-1f9d2e6b4c17") {
		t.Errorf("ClaimID did not reach the response")
	}
	if res.ClientClaim.Value != "sa-east-1" {
		t.Errorf("Value did not reach the response")
	}
}

// RemoveClientClaimRequest carries the addressed entry into its command.
func TestRemoveClientClaimRequest_NamesTheEntry(t *testing.T) {
	r := RemoveClientClaimRequest{ClientClaimID: "01890000-0000-7000-8000-000000000000"}
	if r.ToCommand().ClientClaimID != "01890000-0000-7000-8000-000000000000" {
		t.Error("the entry id did not reach the command, so the wrong entry would be removed")
	}
}

// PatchClientClaimGraphQLRequest carries the entry id from the INPUT.
//
// Same command as its REST twin; the difference is where the id comes from,
// and that difference is the reason this type exists at all.
func TestPatchClientClaimGraphQLRequest_CarriesTheEntryAndItsID(t *testing.T) {
	r := PatchClientClaimGraphQLRequest{ClientClaimID: "01890000-0000-7000-8000-000000000000",
		Value: func() *string { v := string("sa-east-1"); return &v }(),
	}
	cmd := r.ToCommand()
	if cmd.ClientClaimID != "01890000-0000-7000-8000-000000000000" {
		t.Error("the entry id did not reach the command, so the wrong entry would be patched")
	}
	if cmd.Value == nil || *cmd.Value != "sa-east-1" {
		t.Errorf("Value did not reach the command")
	}
}

// RemoveClientClaimGraphQLRequest names the entry through its input.
func TestRemoveClientClaimGraphQLRequest_NamesTheEntry(t *testing.T) {
	r := RemoveClientClaimGraphQLRequest{ClientClaimID: "01890000-0000-7000-8000-000000000000"}
	if r.ToCommand().ClientClaimID != "01890000-0000-7000-8000-000000000000" {
		t.Error("the entry id did not reach the command, so the wrong entry would be removed")
	}
}

// RemoveClientClaimGraphQLResponse acknowledges, since a mutation must answer
// something.
//
// Its REST twin answers 204 with no body at all. This one is the only
// projection in the collection's wire types that is not the generic mapper,
// which is exactly why it is asserted rather than assumed.
func TestRemoveClientClaimGraphQLResponse_Acknowledges(t *testing.T) {
	if !(RemoveClientClaimGraphQLResponse{}).FromResult(fwresults.None{}).Success {
		t.Error("a successful removal answered success: false")
	}
}
