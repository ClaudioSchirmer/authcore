// Code generated by omnicore-gen. DO NOT EDIT.
//
// The per-entry wire types for client_claims.
//
// entity:     Client
// spec:       specs/omnicore-gen/client.omnicore.yaml
// generator:  omnicore-gen
// generated:  2026-08-28
// checksum:   sha256:b94ee902dca7f2ece2068d8a470b23213c3e1f5461487cfbbe197ba0c0bce9d9
//
// The checksum covers this file with the checksum line itself blanked. The
// generator recomputes it before every write: if it does not match, the file
// was changed by hand and the run REFUSES it — your change is neither
// overwritten nor updated. Change the spec and regenerate instead; to keep a
// deliberate edit, run omnicore-gen adopt on this path.

package requests

import (
	"github.com/ClaudioSchirmer/omnicore/domain"

	"github.com/ClaudioSchirmer/authcore/internal/application/commands"
	fwresults "github.com/ClaudioSchirmer/omnicore/application/results"
	fwrequests "github.com/ClaudioSchirmer/omnicore/web/requests"
	fwresponses "github.com/ClaudioSchirmer/omnicore/web/responses"
)

// AddClientClaimRequest adds one entry to an existing Client.
//
// The owner comes from the path; the body is the entry, in the same shape it
// has inside the root's own body.
type AddClientClaimRequest struct {
	fwrequests.Auto
	ClientClaimRequest
}

// ToCommand hands the body to the application layer unchanged. No
// normalisation happens here: the domain is what decides a value's final form.
//
// The embedded marker opts this Request into the framework's generic
// Request→Command mapping: every field travels to the same-named Command
// field, and the pair is checked at boot. A shape that needed renaming or
// reshaping would drop the marker and write ToCommand by hand instead.
func (r AddClientClaimRequest) ToCommand() *commands.AddClientClaimCommand {
	return fwrequests.AutoFromRequest[*commands.AddClientClaimCommand](r)
}

// AddClientClaimResponse is the owner's id plus the entry as stored.
type AddClientClaimResponse struct {
	fwresponses.Auto

	ClientID    domain.ID           `json:"clientId"`
	ClientClaim ClientClaimResponse `json:"clientClaim"`
}

// FromResult projects the application Result onto AddClientClaimResponse.
//
// The embedded marker opts this Response into the framework's generic
// Result→Response mapping: every field is read from the same-named Result
// field, and the pair is checked at boot. A shape that needed renaming or
// reshaping would drop the marker and write FromResult by hand instead.
func (AddClientClaimResponse) FromResult(r commands.AddClientClaimResult) AddClientClaimResponse {
	return fwresponses.AutoFromResult[AddClientClaimResponse](r)
}

// PatchClientClaimRequest changes some fields of one entry, keeping its id.
//
// The entry is named by the clientClaimId path segment. Every field of the
// body is optional: one left out keeps the value the entry already has.
//
// A field the collection put off-limits to a partial change is ABSENT here,
// not ignored — the caller sees what this verb can move, and the entry's
// business identity is decided by what is stored, never by what arrives.
type PatchClientClaimRequest struct {
	fwrequests.Auto
	ClientClaimID string  `path:"clientClaimId"`
	Value         *string `json:"value,omitempty" example:"sa-east-1"`
}

// ToCommand hands the body to the application layer unchanged. No
// normalisation happens here: the domain is what decides a value's final form.
//
// The embedded marker opts this Request into the framework's generic
// Request→Command mapping: every field travels to the same-named Command
// field, and the pair is checked at boot. A shape that needed renaming or
// reshaping would drop the marker and write ToCommand by hand instead.
func (r PatchClientClaimRequest) ToCommand() *commands.PatchClientClaimCommand {
	return fwrequests.AutoFromRequest[*commands.PatchClientClaimCommand](r)
}

// PatchClientClaimResponse is the owner's id plus the entry as stored.
type PatchClientClaimResponse struct {
	fwresponses.Auto

	ClientID    domain.ID           `json:"clientId"`
	ClientClaim ClientClaimResponse `json:"clientClaim"`
}

// FromResult projects the application Result onto PatchClientClaimResponse.
//
// The embedded marker opts this Response into the framework's generic
// Result→Response mapping: every field is read from the same-named Result
// field, and the pair is checked at boot. A shape that needed renaming or
// reshaping would drop the marker and write FromResult by hand instead.
func (PatchClientClaimResponse) FromResult(r commands.PatchClientClaimResult) PatchClientClaimResponse {
	return fwresponses.AutoFromResult[PatchClientClaimResponse](r)
}

// PatchClientClaimGraphQLRequest is PatchClientClaimRequest with the entry's
// id in the input.
//
// GraphQL has no path segment, and the framework's decoder does not fill a
// path-tagged field from an input object. The id travels as a field here,
// under the name the REST route spells in its path, and the command on the
// other side is the same one.
type PatchClientClaimGraphQLRequest struct {
	fwrequests.Auto
	ClientClaimID string  `json:"clientClaimId"`
	Value         *string `json:"value,omitempty" example:"sa-east-1"`
}

// ToCommand hands the body to the application layer unchanged. No
// normalisation happens here: the domain is what decides a value's final form.
//
// The embedded marker opts this Request into the framework's generic
// Request→Command mapping: every field travels to the same-named Command
// field, and the pair is checked at boot. A shape that needed renaming or
// reshaping would drop the marker and write ToCommand by hand instead.
func (r PatchClientClaimGraphQLRequest) ToCommand() *commands.PatchClientClaimCommand {
	return fwrequests.AutoFromRequest[*commands.PatchClientClaimCommand](r)
}

// RemoveClientClaimRequest names the entry to take out.
//
// There is no body: everything the verb needs is in the path. Nothing comes
// back either — the endpoint answers 204.
type RemoveClientClaimRequest struct {
	fwrequests.Auto

	ClientClaimID string `path:"clientClaimId"`
}

// ToCommand hands the body to the application layer unchanged. No
// normalisation happens here: the domain is what decides a value's final form.
//
// The embedded marker opts this Request into the framework's generic
// Request→Command mapping: every field travels to the same-named Command
// field, and the pair is checked at boot. A shape that needed renaming or
// reshaping would drop the marker and write ToCommand by hand instead.
func (r RemoveClientClaimRequest) ToCommand() *commands.RemoveClientClaimCommand {
	return fwrequests.AutoFromRequest[*commands.RemoveClientClaimCommand](r)
}

// RemoveClientClaimGraphQLRequest names the entry to take out.
//
// The entry id is an input field rather than a path segment: GraphQL has no
// path, and the framework's decoder skips a path-tagged field instead of
// inventing a value for it.
type RemoveClientClaimGraphQLRequest struct {
	fwrequests.Auto

	ClientClaimID string `json:"clientClaimId"`
}

// ToCommand hands the body to the application layer unchanged. No
// normalisation happens here: the domain is what decides a value's final form.
//
// The embedded marker opts this Request into the framework's generic
// Request→Command mapping: every field travels to the same-named Command
// field, and the pair is checked at boot. A shape that needed renaming or
// reshaping would drop the marker and write ToCommand by hand instead.
func (r RemoveClientClaimGraphQLRequest) ToCommand() *commands.RemoveClientClaimCommand {
	return fwrequests.AutoFromRequest[*commands.RemoveClientClaimCommand](r)
}

// RemoveClientClaimGraphQLResponse acknowledges the removal, and says nothing
// else.
//
// The REST verb answers 204 with no body. A GraphQL field cannot: it must
// resolve to a type, and a payload with no fields is not publishable. So the
// payload is a single true — the owner id is what the caller passed in, and
// the entry is gone by definition.
//
// No generic mapper here, and it is not an oversight: the command projects the
// framework's None, so there is no Result field for a mapped one to read.
type RemoveClientClaimGraphQLResponse struct {
	Success bool `json:"success"`
}

// FromResult answers true: the pipeline only projects a result it succeeded
// with.
func (RemoveClientClaimGraphQLResponse) FromResult(fwresults.None) RemoveClientClaimGraphQLResponse {
	return RemoveClientClaimGraphQLResponse{Success: true}
}
