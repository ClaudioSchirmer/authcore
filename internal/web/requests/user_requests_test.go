// Code generated by omnicore-gen. DO NOT EDIT.
//
// The request mapper tests.
//
// entity:     User
// spec:       specs/omnicore-gen/user.omnicore.yaml
// generator:  omnicore-gen
// generated:  2026-08-28
// checksum:   sha256:0c4af35e6b7bfe4356e15907d7e80755a9fd722fc7e9a9acca364e7423cd7cab
//
// The checksum covers this file with the checksum line itself blanked. The
// generator recomputes it before every write: if it does not match, the file
// was changed by hand and the run REFUSES it — your change is neither
// overwritten nor updated. Change the spec and regenerate instead; to keep a
// deliberate edit, run omnicore-gen adopt on this path.

package requests

import (
	"testing"

	"github.com/ClaudioSchirmer/authcore/internal/application/commands"
	appqueries "github.com/ClaudioSchirmer/authcore/internal/application/queries"
	fwqueries "github.com/ClaudioSchirmer/omnicore/application/queries"
	fwresults "github.com/ClaudioSchirmer/omnicore/application/results"
	"github.com/ClaudioSchirmer/omnicore/domain"
)

// InsertUserRequest must carry every field into the command.
//
// A field dropped here is invisible: the request parses, the write succeeds,
// and the value the caller sent is not in the row.
func TestInsertUserRequest_CarriesEveryField(t *testing.T) {
	r := InsertUserRequest{
		GivenName:            "Maria",
		FamilyName:           "Souza Lima",
		Email:                "maria@acme.com",
		Status:               "active",
		Password:             "Str0ng!Passphrase",
		PasswordConfirmation: "Str0ng!Passphrase",
		TenantID:             func() *domain.ID { v := domain.ID(domain.NewID("0198f3c2-6b41-7c9e-9f2a-6d3b1e77a410")); return &v }(),
	}
	r.Groups = []UserGroupRequest{{
		GroupID: domain.NewID("0198f3e0-9c25-7a1f-b73d-5e08c4a29f61"),
	}}
	r.Roles = []UserRoleRequest{{
		RoleID: domain.NewID("0198f3e0-1a44-7bb2-9c31-77c0d5e1b904"),
	}}
	cmd := r.ToCommand()
	if cmd == nil {
		t.Fatal("the mapper produced no command")
	}
	if len(cmd.Groups) != 1 {
		t.Errorf("the UserGroup collection did not reach the command")
	}
	if len(cmd.Roles) != 1 {
		t.Errorf("the UserRole collection did not reach the command")
	}
	if cmd.GivenName != r.GivenName {
		t.Errorf("GivenName did not reach the command")
	}
	if cmd.FamilyName != r.FamilyName {
		t.Errorf("FamilyName did not reach the command")
	}
	if cmd.Email != r.Email {
		t.Errorf("Email did not reach the command")
	}
	if cmd.Status != r.Status {
		t.Errorf("Status did not reach the command")
	}
	if cmd.Password != r.Password {
		t.Errorf("Password did not reach the command")
	}
	if cmd.PasswordConfirmation != r.PasswordConfirmation {
		t.Errorf("PasswordConfirmation did not reach the command")
	}
}

// InsertUserResponse must carry the result back out.
//
// The response is the caller's only view of what was written, so a field lost
// here reads as a field that was never saved.
func TestInsertUserResponse_CarriesTheResult(t *testing.T) {
	var in commands.InsertUserResult
	_ = InsertUserResponse{}.FromResult(in)
}

// PatchUserRequest must carry every field into the command.
//
// A field dropped here is invisible: the request parses, the write succeeds,
// and the value the caller sent is not in the row.
func TestPatchUserRequest_CarriesEveryField(t *testing.T) {
	r := PatchUserRequest{
		GivenName:  func() *string { v := string("Maria"); return &v }(),
		FamilyName: func() *string { v := string("Souza Lima"); return &v }(),
		Status:     func() *string { v := string("active"); return &v }(),
	}
	cmd := r.ToCommand()
	if cmd == nil {
		t.Fatal("the mapper produced no command")
	}
}

// PatchUserResponse must carry the result back out.
//
// The response is the caller's only view of what was written, so a field lost
// here reads as a field that was never saved.
func TestPatchUserResponse_CarriesTheResult(t *testing.T) {
	var in commands.PatchUserResult
	_ = PatchUserResponse{}.FromResult(in)
}

// The by-id request maps onto the query it stands for.
//
// It carries the controls the spec declared for a single read; one dropped
// here is a parameter the caller sends and the read ignores.
func TestFindUserByIDRequestBuildsItsQuery(t *testing.T) {
	if q := (FindUserByIDRequest{}).ToQuery(fwqueries.ReadCriteria{}); q == nil {
		t.Fatal("the by-id request produced no query")
	}
	// The criteria travels WHOLE. A by-id read takes it the same way the
	// paged one does — the wrapper parses the wire, the mapper carries it
	// through — so a mapper that rebuilt a fresh criteria instead would
	// drop whatever the wire said, silently and on every request.
	q := (FindUserByIDRequest{}).ToQuery(fwqueries.ReadCriteria{IncludeArchived: true})
	if q == nil || !q.Criteria.IncludeArchived {
		t.Error("the parsed criteria did not reach the query")
	}
}

// The listing request maps onto the query it stands for.
//
// Every control it carries is one the spec declared; one dropped here is a
// parameter the caller sends and the read silently ignores.
func TestFindUsersRequestBuildsItsQuery(t *testing.T) {
	q := FindUsersRequest{}.ToQuery(fwqueries.ReadCriteria{})
	if q == nil {
		t.Fatal("the listing request produced no query")
	}
}

// FindUserByIDResponse is filled from FindUserByIDResult and from nothing
// else.
//
// The mapping is name-based, so a rename on one side and not the other
// produces a field that is silently always empty rather than a compile error.
// The framework catches it at mount; this catches it here, where it is cheap.
func TestFindUserByIDResponse_IsFilledFromTheResult(t *testing.T) {
	out := FindUserByIDResponse{}.FromResult(appqueries.FindUserByIDResult{ID: "7b3c1f10-3c7e-4a8d-9f0e-9d2a8e6d4b51"})
	if out.ID != "7b3c1f10-3c7e-4a8d-9f0e-9d2a8e6d4b51" {
		t.Fatalf("the id did not survive the mapping: %q", out.ID)
	}
}

// FindUsersResponse is filled from FindUsersByParamsResult and from nothing
// else.
//
// The mapping is name-based, so a rename on one side and not the other
// produces a field that is silently always empty rather than a compile error.
// The framework catches it at mount; this catches it here, where it is cheap.
func TestFindUsersResponse_IsFilledFromTheResult(t *testing.T) {
	id := "7b3c1f10-3c7e-4a8d-9f0e-9d2a8e6d4b51"
	out := FindUsersResponse{}.FromResult(appqueries.FindUsersByParamsResult{ID: &id})
	if out.ID == nil || *out.ID != id {
		t.Fatalf("the id did not survive the mapping: %v", out.ID)
	}
}

// AddUserGroupRequest carries the entry into its command.
//
// The body is the same entry shape the root's own body carries, so a field
// forgotten here is saved as missing on a request that answered 201.
func TestAddUserGroupRequest_CarriesEveryField(t *testing.T) {
	r := AddUserGroupRequest{UserGroupRequest: UserGroupRequest{
		GroupID: domain.NewID("0198f3e0-9c25-7a1f-b73d-5e08c4a29f61"),
	}}
	cmd := r.ToCommand()
	if cmd.GroupID != domain.NewID("0198f3e0-9c25-7a1f-b73d-5e08c4a29f61") {
		t.Errorf("GroupID did not reach the command")
	}
}

// The UserGroup responses carry the stored entry back to the caller.
//
// The entry comes back with the id the SERVER minted, which is how the caller
// addresses it afterwards — a response mapper that drops it answers 201 with
// nothing to act on.
func TestAddUserGroupResponse_CarriesTheStoredEntry(t *testing.T) {
	ownerID := domain.NewRandomID()
	entryID := domain.NewRandomID()
	res := AddUserGroupResponse{}.FromResult(commands.AddUserGroupResult{
		UserID: ownerID,
		UserGroup: commands.UserGroupResult{ID: entryID,
			GroupID: domain.NewID("0198f3e0-9c25-7a1f-b73d-5e08c4a29f61"),
		},
	})
	if res.UserID != ownerID {
		t.Error("the owner id did not reach the response")
	}
	if res.UserGroup.ID != entryID {
		t.Error("the entry id the server minted did not reach the response")
	}
	if res.UserGroup.GroupID != domain.NewID("0198f3e0-9c25-7a1f-b73d-5e08c4a29f61") {
		t.Errorf("GroupID did not reach the response")
	}
}

// RemoveUserGroupRequest carries the addressed entry into its command.
func TestRemoveUserGroupRequest_NamesTheEntry(t *testing.T) {
	r := RemoveUserGroupRequest{UserGroupID: "01890000-0000-7000-8000-000000000000"}
	if r.ToCommand().UserGroupID != "01890000-0000-7000-8000-000000000000" {
		t.Error("the entry id did not reach the command, so the wrong entry would be removed")
	}
}

// RemoveUserGroupGraphQLRequest names the entry through its input.
func TestRemoveUserGroupGraphQLRequest_NamesTheEntry(t *testing.T) {
	r := RemoveUserGroupGraphQLRequest{UserGroupID: "01890000-0000-7000-8000-000000000000"}
	if r.ToCommand().UserGroupID != "01890000-0000-7000-8000-000000000000" {
		t.Error("the entry id did not reach the command, so the wrong entry would be removed")
	}
}

// RemoveUserGroupGraphQLResponse acknowledges, since a mutation must answer
// something.
//
// Its REST twin answers 204 with no body at all. This one is the only
// projection in the collection's wire types that is not the generic mapper,
// which is exactly why it is asserted rather than assumed.
func TestRemoveUserGroupGraphQLResponse_Acknowledges(t *testing.T) {
	if !(RemoveUserGroupGraphQLResponse{}).FromResult(fwresults.None{}).Success {
		t.Error("a successful removal answered success: false")
	}
}

// AddUserRoleRequest carries the entry into its command.
//
// The body is the same entry shape the root's own body carries, so a field
// forgotten here is saved as missing on a request that answered 201.
func TestAddUserRoleRequest_CarriesEveryField(t *testing.T) {
	r := AddUserRoleRequest{UserRoleRequest: UserRoleRequest{
		RoleID: domain.NewID("0198f3e0-1a44-7bb2-9c31-77c0d5e1b904"),
	}}
	cmd := r.ToCommand()
	if cmd.RoleID != domain.NewID("0198f3e0-1a44-7bb2-9c31-77c0d5e1b904") {
		t.Errorf("RoleID did not reach the command")
	}
}

// The UserRole responses carry the stored entry back to the caller.
//
// The entry comes back with the id the SERVER minted, which is how the caller
// addresses it afterwards — a response mapper that drops it answers 201 with
// nothing to act on.
func TestAddUserRoleResponse_CarriesTheStoredEntry(t *testing.T) {
	ownerID := domain.NewRandomID()
	entryID := domain.NewRandomID()
	res := AddUserRoleResponse{}.FromResult(commands.AddUserRoleResult{
		UserID: ownerID,
		UserRole: commands.UserRoleResult{ID: entryID,
			RoleID: domain.NewID("0198f3e0-1a44-7bb2-9c31-77c0d5e1b904"),
		},
	})
	if res.UserID != ownerID {
		t.Error("the owner id did not reach the response")
	}
	if res.UserRole.ID != entryID {
		t.Error("the entry id the server minted did not reach the response")
	}
	if res.UserRole.RoleID != domain.NewID("0198f3e0-1a44-7bb2-9c31-77c0d5e1b904") {
		t.Errorf("RoleID did not reach the response")
	}
}

// RemoveUserRoleRequest carries the addressed entry into its command.
func TestRemoveUserRoleRequest_NamesTheEntry(t *testing.T) {
	r := RemoveUserRoleRequest{UserRoleID: "01890000-0000-7000-8000-000000000000"}
	if r.ToCommand().UserRoleID != "01890000-0000-7000-8000-000000000000" {
		t.Error("the entry id did not reach the command, so the wrong entry would be removed")
	}
}

// RemoveUserRoleGraphQLRequest names the entry through its input.
func TestRemoveUserRoleGraphQLRequest_NamesTheEntry(t *testing.T) {
	r := RemoveUserRoleGraphQLRequest{UserRoleID: "01890000-0000-7000-8000-000000000000"}
	if r.ToCommand().UserRoleID != "01890000-0000-7000-8000-000000000000" {
		t.Error("the entry id did not reach the command, so the wrong entry would be removed")
	}
}

// RemoveUserRoleGraphQLResponse acknowledges, since a mutation must answer
// something.
//
// Its REST twin answers 204 with no body at all. This one is the only
// projection in the collection's wire types that is not the generic mapper,
// which is exactly why it is asserted rather than assumed.
func TestRemoveUserRoleGraphQLResponse_Acknowledges(t *testing.T) {
	if !(RemoveUserRoleGraphQLResponse{}).FromResult(fwresults.None{}).Success {
		t.Error("a successful removal answered success: false")
	}
}
