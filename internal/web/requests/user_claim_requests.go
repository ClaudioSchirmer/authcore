// Code generated by omnicore-gen. DO NOT EDIT.
//
// The per-entry wire types for user_claims.
//
// entity:     User
// spec:       specs/omnicore-gen/user.omnicore.yaml
// generator:  omnicore-gen
// generated:  2026-08-29
// checksum:   sha256:fd8451661f00edf5fac2970a95a5a8b105d8835b331852ca10e37d5f6aae7554
//
// The line above is the Go convention that tells linters to skip this file.
// It is NOT a rule that the code may not change: this file is yours, in your
// repository, and improving it is ordinary work. What the generator asks is
// only that it be told.
//
// Changing the SPEC and regenerating is the cheaper of the two edits — it
// survives every later run. When the spec cannot say what you need and the
// framework can, edit this file: ask whoever owns the service first, and
// then run `omnicore-gen adopt <this path> -why '<what the spec could not
// express>'`. Adopting re-hashes the file as it now stands, so regeneration
// KEEPS your edit — and every later run prints the file as adopted, so it
// stays visible instead of becoming a surprise.
//
// Without that adopt, the next run notices the checksum no longer matches
// and REFUSES rather than overwriting — your work is never lost, but the
// run stops for whoever meets it.

package requests

import (
	"github.com/ClaudioSchirmer/omnicore/domain"

	"github.com/ClaudioSchirmer/authcore/internal/application/commands"
	fwresults "github.com/ClaudioSchirmer/omnicore/application/results"
	fwrequests "github.com/ClaudioSchirmer/omnicore/web/requests"
	fwresponses "github.com/ClaudioSchirmer/omnicore/web/responses"
)

// AddUserClaimRequest adds one entry to an existing User.
//
// The owner comes from the path; the body is the entry, in the same shape it
// has inside the root's own body.
type AddUserClaimRequest struct {
	fwrequests.Auto
	UserClaimRequest
}

// ToCommand hands the body to the application layer unchanged. No
// normalisation happens here: the domain is what decides a value's final form.
//
// The embedded marker opts this Request into the framework's generic
// Request→Command mapping: every field travels to the same-named Command
// field, and the pair is checked at boot. A shape that needed renaming or
// reshaping would drop the marker and write ToCommand by hand instead.
func (r AddUserClaimRequest) ToCommand() *commands.AddUserClaimCommand {
	return fwrequests.AutoFromRequest[*commands.AddUserClaimCommand](r)
}

// AddUserClaimResponse is the owner's id plus the entry as stored.
type AddUserClaimResponse struct {
	fwresponses.Auto

	UserID    domain.ID         `json:"userId"`
	UserClaim UserClaimResponse `json:"userClaim"`
}

// FromResult projects the application Result onto AddUserClaimResponse.
//
// The embedded marker opts this Response into the framework's generic
// Result→Response mapping: every field is read from the same-named Result
// field, and the pair is checked at boot. A shape that needed renaming or
// reshaping would drop the marker and write FromResult by hand instead.
func (AddUserClaimResponse) FromResult(r commands.AddUserClaimResult) AddUserClaimResponse {
	return fwresponses.AutoFromResult[AddUserClaimResponse](r)
}

// PatchUserClaimRequest changes some fields of one entry, keeping its id.
//
// The entry is named by the userClaimId path segment. Every field of the body
// is optional: one left out keeps the value the entry already has.
//
// A field the collection put off-limits to a partial change is ABSENT here,
// not ignored — the caller sees what this verb can move, and the entry's
// business identity is decided by what is stored, never by what arrives.
type PatchUserClaimRequest struct {
	fwrequests.Auto
	UserClaimID string  `path:"userClaimId"`
	Value       *string `json:"value,omitempty" example:"1000"`
}

// ToCommand hands the body to the application layer unchanged. No
// normalisation happens here: the domain is what decides a value's final form.
//
// The embedded marker opts this Request into the framework's generic
// Request→Command mapping: every field travels to the same-named Command
// field, and the pair is checked at boot. A shape that needed renaming or
// reshaping would drop the marker and write ToCommand by hand instead.
func (r PatchUserClaimRequest) ToCommand() *commands.PatchUserClaimCommand {
	return fwrequests.AutoFromRequest[*commands.PatchUserClaimCommand](r)
}

// PatchUserClaimResponse is the owner's id plus the entry as stored.
type PatchUserClaimResponse struct {
	fwresponses.Auto

	UserID    domain.ID         `json:"userId"`
	UserClaim UserClaimResponse `json:"userClaim"`
}

// FromResult projects the application Result onto PatchUserClaimResponse.
//
// The embedded marker opts this Response into the framework's generic
// Result→Response mapping: every field is read from the same-named Result
// field, and the pair is checked at boot. A shape that needed renaming or
// reshaping would drop the marker and write FromResult by hand instead.
func (PatchUserClaimResponse) FromResult(r commands.PatchUserClaimResult) PatchUserClaimResponse {
	return fwresponses.AutoFromResult[PatchUserClaimResponse](r)
}

// PatchUserClaimGraphQLRequest is PatchUserClaimRequest with the entry's id in
// the input.
//
// GraphQL has no path segment, and the framework's decoder does not fill a
// path-tagged field from an input object. The id travels as a field here,
// under the name the REST route spells in its path, and the command on the
// other side is the same one.
type PatchUserClaimGraphQLRequest struct {
	fwrequests.Auto
	UserClaimID string  `json:"userClaimId"`
	Value       *string `json:"value,omitempty" example:"1000"`
}

// ToCommand hands the body to the application layer unchanged. No
// normalisation happens here: the domain is what decides a value's final form.
//
// The embedded marker opts this Request into the framework's generic
// Request→Command mapping: every field travels to the same-named Command
// field, and the pair is checked at boot. A shape that needed renaming or
// reshaping would drop the marker and write ToCommand by hand instead.
func (r PatchUserClaimGraphQLRequest) ToCommand() *commands.PatchUserClaimCommand {
	return fwrequests.AutoFromRequest[*commands.PatchUserClaimCommand](r)
}

// RemoveUserClaimRequest names the entry to take out.
//
// There is no body: everything the verb needs is in the path. Nothing comes
// back either — the endpoint answers 204.
type RemoveUserClaimRequest struct {
	fwrequests.Auto

	UserClaimID string `path:"userClaimId"`
}

// ToCommand hands the body to the application layer unchanged. No
// normalisation happens here: the domain is what decides a value's final form.
//
// The embedded marker opts this Request into the framework's generic
// Request→Command mapping: every field travels to the same-named Command
// field, and the pair is checked at boot. A shape that needed renaming or
// reshaping would drop the marker and write ToCommand by hand instead.
func (r RemoveUserClaimRequest) ToCommand() *commands.RemoveUserClaimCommand {
	return fwrequests.AutoFromRequest[*commands.RemoveUserClaimCommand](r)
}

// RemoveUserClaimGraphQLRequest names the entry to take out.
//
// The entry id is an input field rather than a path segment: GraphQL has no
// path, and the framework's decoder skips a path-tagged field instead of
// inventing a value for it.
type RemoveUserClaimGraphQLRequest struct {
	fwrequests.Auto

	UserClaimID string `json:"userClaimId"`
}

// ToCommand hands the body to the application layer unchanged. No
// normalisation happens here: the domain is what decides a value's final form.
//
// The embedded marker opts this Request into the framework's generic
// Request→Command mapping: every field travels to the same-named Command
// field, and the pair is checked at boot. A shape that needed renaming or
// reshaping would drop the marker and write ToCommand by hand instead.
func (r RemoveUserClaimGraphQLRequest) ToCommand() *commands.RemoveUserClaimCommand {
	return fwrequests.AutoFromRequest[*commands.RemoveUserClaimCommand](r)
}

// RemoveUserClaimGraphQLResponse acknowledges the removal, and says nothing
// else.
//
// The REST verb answers 204 with no body. A GraphQL field cannot: it must
// resolve to a type, and a payload with no fields is not publishable. So the
// payload is a single true — the owner id is what the caller passed in, and
// the entry is gone by definition.
//
// No generic mapper here, and it is not an oversight: the command projects the
// framework's None, so there is no Result field for a mapped one to read.
type RemoveUserClaimGraphQLResponse struct {
	Success bool `json:"success"`
}

// FromResult answers true: the pipeline only projects a result it succeeded
// with.
func (RemoveUserClaimGraphQLResponse) FromResult(fwresults.None) RemoveUserClaimGraphQLResponse {
	return RemoveUserClaimGraphQLResponse{Success: true}
}
