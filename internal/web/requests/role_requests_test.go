// Code generated by omnicore-gen. DO NOT EDIT.
//
// The request mapper tests.
//
// entity:     Role
// spec:       specs/omnicore-gen/role.omnicore.yaml
// generator:  omnicore-gen
// generated:  2026-08-24
// checksum:   sha256:951c0b289c2012ad28da1059eab10a8a26de627e4f222c914305d47ea5c6a957
//
// The checksum covers this file with the checksum line itself blanked. The
// generator recomputes it before every write: if it does not match, the file
// was changed by hand and the run REFUSES it — your change is neither
// overwritten nor updated. Change the spec and regenerate instead; to keep a
// deliberate edit, run omnicore-gen adopt on this path.

package requests

import (
	"testing"

	"github.com/ClaudioSchirmer/authcore/internal/application/commands"
	appqueries "github.com/ClaudioSchirmer/authcore/internal/application/queries"
	fwqueries "github.com/ClaudioSchirmer/omnicore/application/queries"
	"github.com/ClaudioSchirmer/omnicore/domain"
)

// InsertRoleRequest must carry every field into the command.
//
// A field dropped here is invisible: the request parses, the write succeeds,
// and the value the caller sent is not in the row.
func TestInsertRoleRequest_CarriesEveryField(t *testing.T) {
	r := InsertRoleRequest{
		TenantID:    domain.NewID("0198f3c2-6b41-7c9e-9f2a-6d3b1e77a410"),
		Key:         "billing-manager",
		Name:        "Billing Manager",
		Description: "Grants read access to the tenant registry and the permission catalog, without any write verb.",
	}
	r.Permissions = []RolePermissionRequest{{
		PermissionID: domain.NewID("0198f3d4-1a77-7b52-8e04-2c9f5a13d6b8"),
	}}
	cmd := r.ToCommand()
	if cmd == nil {
		t.Fatal("the mapper produced no command")
	}
	if len(cmd.Permissions) != 1 {
		t.Errorf("the RolePermission collection did not reach the command")
	}
	if cmd.TenantID != r.TenantID {
		t.Errorf("TenantID did not reach the command")
	}
	if cmd.Key != r.Key {
		t.Errorf("Key did not reach the command")
	}
	if cmd.Name != r.Name {
		t.Errorf("Name did not reach the command")
	}
	if cmd.Description != r.Description {
		t.Errorf("Description did not reach the command")
	}
}

// InsertRoleResponse must carry the result back out.
//
// The response is the caller's only view of what was written, so a field lost
// here reads as a field that was never saved.
func TestInsertRoleResponse_CarriesTheResult(t *testing.T) {
	var in commands.InsertRoleResult
	_ = InsertRoleResponse{}.FromResult(in)
}

// PatchRoleRequest must carry every field into the command.
//
// A field dropped here is invisible: the request parses, the write succeeds,
// and the value the caller sent is not in the row.
func TestPatchRoleRequest_CarriesEveryField(t *testing.T) {
	r := PatchRoleRequest{
		TenantID: func() *domain.ID { v := domain.ID(domain.NewID("0198f3c2-6b41-7c9e-9f2a-6d3b1e77a410")); return &v }(),
		Key:      func() *string { v := string("billing-manager"); return &v }(),
		Name:     func() *string { v := string("Billing Manager"); return &v }(),
		Description: func() *string {
			v := string("Grants read access to the tenant registry and the permission catalog, without any write verb.")
			return &v
		}(),
	}
	cmd := r.ToCommand()
	if cmd == nil {
		t.Fatal("the mapper produced no command")
	}
}

// PatchRoleResponse must carry the result back out.
//
// The response is the caller's only view of what was written, so a field lost
// here reads as a field that was never saved.
func TestPatchRoleResponse_CarriesTheResult(t *testing.T) {
	var in commands.PatchRoleResult
	_ = PatchRoleResponse{}.FromResult(in)
}

// The by-id request maps onto the query it stands for.
//
// It carries the controls the spec declared for a single read; one dropped
// here is a parameter the caller sends and the read ignores.
func TestFindRoleByIDRequestBuildsItsQuery(t *testing.T) {
	if q := (FindRoleByIDRequest{}).ToQuery(fwqueries.ReadCriteria{}); q == nil {
		t.Fatal("the by-id request produced no query")
	}
	// The criteria travels WHOLE. A by-id read takes it the same way the
	// paged one does — the wrapper parses the wire, the mapper carries it
	// through — so a mapper that rebuilt a fresh criteria instead would
	// drop whatever the wire said, silently and on every request.
	q := (FindRoleByIDRequest{}).ToQuery(fwqueries.ReadCriteria{IncludeArchived: true})
	if q == nil || !q.Criteria.IncludeArchived {
		t.Error("the parsed criteria did not reach the query")
	}
}

// The listing request maps onto the query it stands for.
//
// Every control it carries is one the spec declared; one dropped here is a
// parameter the caller sends and the read silently ignores.
func TestFindRolesRequestBuildsItsQuery(t *testing.T) {
	q := FindRolesRequest{}.ToQuery(fwqueries.ReadCriteria{})
	if q == nil {
		t.Fatal("the listing request produced no query")
	}
}

// FindRoleByIDResponse is filled from FindRoleByIDResult and from nothing
// else.
//
// The mapping is name-based, so a rename on one side and not the other
// produces a field that is silently always empty rather than a compile error.
// The framework catches it at mount; this catches it here, where it is cheap.
func TestFindRoleByIDResponse_IsFilledFromTheResult(t *testing.T) {
	out := FindRoleByIDResponse{}.FromResult(appqueries.FindRoleByIDResult{ID: "7b3c1f10-3c7e-4a8d-9f0e-9d2a8e6d4b51"})
	if out.ID != "7b3c1f10-3c7e-4a8d-9f0e-9d2a8e6d4b51" {
		t.Fatalf("the id did not survive the mapping: %q", out.ID)
	}
}

// FindRolesResponse is filled from FindRolesByParamsResult and from nothing
// else.
//
// The mapping is name-based, so a rename on one side and not the other
// produces a field that is silently always empty rather than a compile error.
// The framework catches it at mount; this catches it here, where it is cheap.
func TestFindRolesResponse_IsFilledFromTheResult(t *testing.T) {
	id := "7b3c1f10-3c7e-4a8d-9f0e-9d2a8e6d4b51"
	out := FindRolesResponse{}.FromResult(appqueries.FindRolesByParamsResult{ID: &id})
	if out.ID == nil || *out.ID != id {
		t.Fatalf("the id did not survive the mapping: %v", out.ID)
	}
}

// AddRolePermissionRequest carries the entry into its command.
//
// The body is the same entry shape the root's own body carries, so a field
// forgotten here is saved as missing on a request that answered 201.
func TestAddRolePermissionRequest_CarriesEveryField(t *testing.T) {
	r := AddRolePermissionRequest{RolePermissionRequest: RolePermissionRequest{
		PermissionID: domain.NewID("0198f3d4-1a77-7b52-8e04-2c9f5a13d6b8"),
	}}
	cmd := r.ToCommand()
	if cmd.PermissionID != domain.NewID("0198f3d4-1a77-7b52-8e04-2c9f5a13d6b8") {
		t.Errorf("PermissionID did not reach the command")
	}
}

// The RolePermission responses carry the stored entry back to the caller.
//
// The entry comes back with the id the SERVER minted, which is how the caller
// addresses it afterwards — a response mapper that drops it answers 201 with
// nothing to act on.
func TestAddRolePermissionResponse_CarriesTheStoredEntry(t *testing.T) {
	ownerID := domain.NewRandomID()
	entryID := domain.NewRandomID()
	res := AddRolePermissionResponse{}.FromResult(commands.AddRolePermissionResult{
		RoleID: ownerID,
		RolePermission: commands.RolePermissionResult{ID: entryID,
			PermissionID: domain.NewID("0198f3d4-1a77-7b52-8e04-2c9f5a13d6b8"),
		},
	})
	if res.RoleID != ownerID {
		t.Error("the owner id did not reach the response")
	}
	if res.RolePermission.ID != entryID {
		t.Error("the entry id the server minted did not reach the response")
	}
	if res.RolePermission.PermissionID != domain.NewID("0198f3d4-1a77-7b52-8e04-2c9f5a13d6b8") {
		t.Errorf("PermissionID did not reach the response")
	}
}

// RemoveRolePermission answers with the owner, which is all it has to carry.
func TestRemoveRolePermissionRequestAndResponse(t *testing.T) {
	r := RemoveRolePermissionRequest{RolePermissionID: "01890000-0000-7000-8000-000000000000"}
	if r.ToCommand().RolePermissionID != "01890000-0000-7000-8000-000000000000" {
		t.Error("the entry id did not reach the command, so the wrong entry would be removed")
	}
	ownerID := domain.NewRandomID()
	if (RemoveRolePermissionResponse{}).FromResult(commands.RemoveRolePermissionResult{RoleID: ownerID}).RoleID != ownerID {
		t.Error("the owner id did not reach the response")
	}
}
