// Code generated by omnicore-gen. DO NOT EDIT.
//
// The archive wire pair for one user_roles entry.
//
// entity:     User
// spec:       specs/omnicore-gen/user.omnicore.yaml
// generator:  omnicore-gen
// generated:  2026-09-01
// checksum:   sha256:aeee734fdd40120ab8ba749587341a57e6721ac51cb0982a60b627902d4373d4
//
// The line above is the Go convention that tells linters to skip this file.
// It is NOT a rule that the code may not change: this file is yours, in your
// repository, and improving it is ordinary work. What the generator asks is
// only that it be told.
//
// Changing the SPEC and regenerating is the cheaper of the two edits — it
// survives every later run. When the spec cannot say what you need and the
// framework can, edit this file: ask whoever owns the service first, and
// then run `omnicore-gen adopt <this path> -why '<what the spec could not
// express>'`. Adopting re-hashes the file as it now stands, so regeneration
// KEEPS your edit — and every later run prints the file as adopted, so it
// stays visible instead of becoming a surprise.
//
// Without that adopt, the next run notices the checksum no longer matches
// and REFUSES rather than overwriting — your work is never lost, but the
// run stops for whoever meets it.

package requests

import (
	fwrequests "github.com/ClaudioSchirmer/omnicore/web/requests"

	"github.com/ClaudioSchirmer/authcore/internal/application/commands"
	fwresults "github.com/ClaudioSchirmer/omnicore/application/results"
)

// ArchiveUserRoleRequest names the entry to take out.
//
// There is no body: everything the verb needs is in the path. Nothing comes
// back either — the endpoint answers 204.
type ArchiveUserRoleRequest struct {
	fwrequests.Auto

	UserRoleID string `path:"userRoleId"`
}

// ToCommand hands the body to the application layer unchanged. No
// normalisation happens here: the domain is what decides a value's final form.
//
// The embedded marker opts this Request into the framework's generic
// Request→Command mapping: every field travels to the same-named Command
// field, and the pair is checked at boot. A shape that needed renaming or
// reshaping would drop the marker and write ToCommand by hand instead.
func (r ArchiveUserRoleRequest) ToCommand() *commands.ArchiveUserRoleCommand {
	return fwrequests.AutoFromRequest[*commands.ArchiveUserRoleCommand](r)
}

// ArchiveUserRoleGraphQLRequest names the entry to take out.
//
// The entry id is an input field rather than a path segment: GraphQL has no
// path, and the framework's decoder skips a path-tagged field instead of
// inventing a value for it.
type ArchiveUserRoleGraphQLRequest struct {
	fwrequests.Auto

	UserRoleID string `json:"userRoleId"`
}

// ToCommand hands the body to the application layer unchanged. No
// normalisation happens here: the domain is what decides a value's final form.
//
// The embedded marker opts this Request into the framework's generic
// Request→Command mapping: every field travels to the same-named Command
// field, and the pair is checked at boot. A shape that needed renaming or
// reshaping would drop the marker and write ToCommand by hand instead.
func (r ArchiveUserRoleGraphQLRequest) ToCommand() *commands.ArchiveUserRoleCommand {
	return fwrequests.AutoFromRequest[*commands.ArchiveUserRoleCommand](r)
}

// ArchiveUserRoleGraphQLResponse acknowledges the removal, and says nothing
// else.
//
// The REST verb answers 204 with no body. A GraphQL field cannot: it must
// resolve to a type, and a payload with no fields is not publishable. So the
// payload is a single true — the owner id is what the caller passed in, and
// the entry is gone by definition.
//
// No generic mapper here, and it is not an oversight: the command projects the
// framework's None, so there is no Result field for a mapped one to read.
type ArchiveUserRoleGraphQLResponse struct {
	Success bool `json:"success"`
}

// FromResult answers true: the pipeline only projects a result it succeeded
// with.
func (ArchiveUserRoleGraphQLResponse) FromResult(fwresults.None) ArchiveUserRoleGraphQLResponse {
	return ArchiveUserRoleGraphQLResponse{Success: true}
}
