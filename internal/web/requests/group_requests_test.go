// Code generated by omnicore-gen. DO NOT EDIT.
//
// The request mapper tests.
//
// entity:     Group
// spec:       specs/omnicore-gen/group.omnicore.yaml
// generator:  omnicore-gen
// generated:  2026-08-24
// checksum:   sha256:2cca7dae030aa5b9c095072fb035c46a7001736cfa1d58ea8d1a5ea9cda0de84
//
// The checksum covers this file with the checksum line itself blanked. The
// generator recomputes it before every write: if it does not match, the file
// was changed by hand and the run REFUSES it — your change is neither
// overwritten nor updated. Change the spec and regenerate instead; to keep a
// deliberate edit, run omnicore-gen adopt on this path.

package requests

import (
	"testing"

	"github.com/ClaudioSchirmer/authcore/internal/application/commands"
	appqueries "github.com/ClaudioSchirmer/authcore/internal/application/queries"
	fwqueries "github.com/ClaudioSchirmer/omnicore/application/queries"
	"github.com/ClaudioSchirmer/omnicore/domain"
)

// InsertGroupRequest must carry every field into the command.
//
// A field dropped here is invisible: the request parses, the write succeeds,
// and the value the caller sent is not in the row.
func TestInsertGroupRequest_CarriesEveryField(t *testing.T) {
	r := InsertGroupRequest{
		TenantID:    domain.NewID("0198f3c2-6b41-7c9e-9f2a-6d3b1e77a410"),
		Key:         "engineering",
		Name:        "Engineering",
		Description: "Everyone in the product engineering org: read access to the tenant registry and the permission catalog, plus deploy rights.",
	}
	r.Roles = []GroupRoleRequest{{
		RoleID: domain.NewID("0198f3e0-9c25-7a1f-b73d-5e08c4a29f61"),
	}}
	cmd := r.ToCommand()
	if cmd == nil {
		t.Fatal("the mapper produced no command")
	}
	if len(cmd.Roles) != 1 {
		t.Errorf("the GroupRole collection did not reach the command")
	}
	if cmd.TenantID != r.TenantID {
		t.Errorf("TenantID did not reach the command")
	}
	if cmd.Key != r.Key {
		t.Errorf("Key did not reach the command")
	}
	if cmd.Name != r.Name {
		t.Errorf("Name did not reach the command")
	}
	if cmd.Description != r.Description {
		t.Errorf("Description did not reach the command")
	}
}

// InsertGroupResponse must carry the result back out.
//
// The response is the caller's only view of what was written, so a field lost
// here reads as a field that was never saved.
func TestInsertGroupResponse_CarriesTheResult(t *testing.T) {
	var in commands.InsertGroupResult
	_ = InsertGroupResponse{}.FromResult(in)
}

// PatchGroupRequest must carry every field into the command.
//
// A field dropped here is invisible: the request parses, the write succeeds,
// and the value the caller sent is not in the row.
func TestPatchGroupRequest_CarriesEveryField(t *testing.T) {
	r := PatchGroupRequest{
		TenantID: func() *domain.ID { v := domain.ID(domain.NewID("0198f3c2-6b41-7c9e-9f2a-6d3b1e77a410")); return &v }(),
		Key:      func() *string { v := string("engineering"); return &v }(),
		Name:     func() *string { v := string("Engineering"); return &v }(),
		Description: func() *string {
			v := string("Everyone in the product engineering org: read access to the tenant registry and the permission catalog, plus deploy rights.")
			return &v
		}(),
	}
	cmd := r.ToCommand()
	if cmd == nil {
		t.Fatal("the mapper produced no command")
	}
}

// PatchGroupResponse must carry the result back out.
//
// The response is the caller's only view of what was written, so a field lost
// here reads as a field that was never saved.
func TestPatchGroupResponse_CarriesTheResult(t *testing.T) {
	var in commands.PatchGroupResult
	_ = PatchGroupResponse{}.FromResult(in)
}

// The by-id request maps onto the query it stands for.
//
// It carries the controls the spec declared for a single read; one dropped
// here is a parameter the caller sends and the read ignores.
func TestFindGroupByIDRequestBuildsItsQuery(t *testing.T) {
	if q := (FindGroupByIDRequest{}).ToQuery(fwqueries.ReadCriteria{}); q == nil {
		t.Fatal("the by-id request produced no query")
	}
	// The criteria travels WHOLE. A by-id read takes it the same way the
	// paged one does — the wrapper parses the wire, the mapper carries it
	// through — so a mapper that rebuilt a fresh criteria instead would
	// drop whatever the wire said, silently and on every request.
	q := (FindGroupByIDRequest{}).ToQuery(fwqueries.ReadCriteria{IncludeArchived: true})
	if q == nil || !q.Criteria.IncludeArchived {
		t.Error("the parsed criteria did not reach the query")
	}
}

// The listing request maps onto the query it stands for.
//
// Every control it carries is one the spec declared; one dropped here is a
// parameter the caller sends and the read silently ignores.
func TestFindGroupsRequestBuildsItsQuery(t *testing.T) {
	q := FindGroupsRequest{}.ToQuery(fwqueries.ReadCriteria{})
	if q == nil {
		t.Fatal("the listing request produced no query")
	}
}

// FindGroupByIDResponse is filled from FindGroupByIDResult and from nothing
// else.
//
// The mapping is name-based, so a rename on one side and not the other
// produces a field that is silently always empty rather than a compile error.
// The framework catches it at mount; this catches it here, where it is cheap.
func TestFindGroupByIDResponse_IsFilledFromTheResult(t *testing.T) {
	out := FindGroupByIDResponse{}.FromResult(appqueries.FindGroupByIDResult{ID: "7b3c1f10-3c7e-4a8d-9f0e-9d2a8e6d4b51"})
	if out.ID != "7b3c1f10-3c7e-4a8d-9f0e-9d2a8e6d4b51" {
		t.Fatalf("the id did not survive the mapping: %q", out.ID)
	}
}

// FindGroupsResponse is filled from FindGroupsByParamsResult and from nothing
// else.
//
// The mapping is name-based, so a rename on one side and not the other
// produces a field that is silently always empty rather than a compile error.
// The framework catches it at mount; this catches it here, where it is cheap.
func TestFindGroupsResponse_IsFilledFromTheResult(t *testing.T) {
	id := "7b3c1f10-3c7e-4a8d-9f0e-9d2a8e6d4b51"
	out := FindGroupsResponse{}.FromResult(appqueries.FindGroupsByParamsResult{ID: &id})
	if out.ID == nil || *out.ID != id {
		t.Fatalf("the id did not survive the mapping: %v", out.ID)
	}
}

// AddGroupRoleRequest carries the entry into its command.
//
// The body is the same entry shape the root's own body carries, so a field
// forgotten here is saved as missing on a request that answered 201.
func TestAddGroupRoleRequest_CarriesEveryField(t *testing.T) {
	r := AddGroupRoleRequest{GroupRoleRequest: GroupRoleRequest{
		RoleID: domain.NewID("0198f3e0-9c25-7a1f-b73d-5e08c4a29f61"),
	}}
	cmd := r.ToCommand()
	if cmd.RoleID != domain.NewID("0198f3e0-9c25-7a1f-b73d-5e08c4a29f61") {
		t.Errorf("RoleID did not reach the command")
	}
}

// The GroupRole responses carry the stored entry back to the caller.
//
// The entry comes back with the id the SERVER minted, which is how the caller
// addresses it afterwards — a response mapper that drops it answers 201 with
// nothing to act on.
func TestAddGroupRoleResponse_CarriesTheStoredEntry(t *testing.T) {
	ownerID := domain.NewRandomID()
	entryID := domain.NewRandomID()
	res := AddGroupRoleResponse{}.FromResult(commands.AddGroupRoleResult{
		GroupID: ownerID,
		GroupRole: commands.GroupRoleResult{ID: entryID,
			RoleID: domain.NewID("0198f3e0-9c25-7a1f-b73d-5e08c4a29f61"),
		},
	})
	if res.GroupID != ownerID {
		t.Error("the owner id did not reach the response")
	}
	if res.GroupRole.ID != entryID {
		t.Error("the entry id the server minted did not reach the response")
	}
	if res.GroupRole.RoleID != domain.NewID("0198f3e0-9c25-7a1f-b73d-5e08c4a29f61") {
		t.Errorf("RoleID did not reach the response")
	}
}

// RemoveGroupRole answers with the owner, which is all it has to carry.
func TestRemoveGroupRoleRequestAndResponse(t *testing.T) {
	r := RemoveGroupRoleRequest{GroupRoleID: "01890000-0000-7000-8000-000000000000"}
	if r.ToCommand().GroupRoleID != "01890000-0000-7000-8000-000000000000" {
		t.Error("the entry id did not reach the command, so the wrong entry would be removed")
	}
	ownerID := domain.NewRandomID()
	if (RemoveGroupRoleResponse{}).FromResult(commands.RemoveGroupRoleResult{GroupID: ownerID}).GroupID != ownerID {
		t.Error("the owner id did not reach the response")
	}
}
